---
phase: 08-home-hub
plan: 04
type: tdd
wave: 2
depends_on: ["08-00", "08-03"]
files_modified:
  - tests/components/TransactionItem.test.tsx
  - src/components/dashboard/TransactionItem.tsx
  - src/components/dashboard/index.ts
autonomous: true
requirements: [PAGE-03]
tags: [phase-8, home-hub, transaction-item, tdd, server-component, ui]

must_haves:
  truths:
    - "TransactionItem renders merchant, amount, category, and date in the locked layout"
    - "Expense amounts render with U+2212 MINUS SIGN (−), not hyphen-minus (-)"
    - "Income amounts render with + prefix and text-bb-positive color"
    - "Expense amounts render with − prefix and text-bb-negative color"
    - "Component is a pure server component (no client directive, no hooks)"
  artifacts:
    - path: "src/components/dashboard/TransactionItem.tsx"
      provides: "Presentational view-model component (PAGE-03)"
      exports: ["TransactionItem", "TransactionItemProps"]
      contains: "export function TransactionItem"
      min_lines: 50
    - path: "src/components/dashboard/index.ts"
      provides: "Barrel re-export updated with TransactionItem"
      contains: "export { TransactionItem }"
    - path: "tests/components/TransactionItem.test.tsx"
      provides: "Render test coverage"
      contains: "describe(\"TransactionItem\""
      min_lines: 80
  key_links:
    - from: "src/components/dashboard/TransactionItem.tsx"
      to: "@/utils/currency"
      via: "import statement"
      pattern: "from \"@/utils/currency\""
    - from: "src/components/dashboard/TransactionItem.tsx"
      to: "@/lib/utils (cn)"
      via: "import statement"
      pattern: "import \\{ cn \\}"
---

<objective>
Implement `src/components/dashboard/TransactionItem.tsx` — the single-row presentational component used on the Home hub (Phase 8) and Transactions page (Phase 9 — not in this plan). View-model interface locked by D-CMP-02. Visual contract locked by 08-UI-SPEC.md § "Section 3: Recent Transactions" and "TransactionItem visual contract".

The component is a **server component** (no interactivity, no hooks) and uses ONLY `--bb-*` Tailwind utility tokens (no hardcoded colors). TDD: render-tests-first.

Purpose: PAGE-03 requires the TransactionItem pattern (merchant + amount + category + date, no icons, consistent height). D-CMP-02 locks the view-model shape so the parent page does DB-row → view-model mapping, decoupling the component from the DB schema.

Output:
- `tests/components/TransactionItem.test.tsx` with ≥ 6 `it()` cases
- `src/components/dashboard/TransactionItem.tsx` with named export `TransactionItem` and exported interface `TransactionItemProps`
- `src/components/dashboard/index.ts` updated with the new re-export
- 2 TDD commits (RED → GREEN)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/08-home-hub/08-CONTEXT.md
@.planning/phases/08-home-hub/08-RESEARCH.md
@.planning/phases/08-home-hub/08-PATTERNS.md
@.planning/phases/08-home-hub/08-UI-SPEC.md
@src/app/(bb)/page.tsx
@src/components/dashboard/SyncTransactionsButton.tsx
@src/components/dashboard/index.ts
@src/types/finance.ts

<interfaces>
<!-- Public API (locked by D-CMP-02): -->

export interface TransactionItemProps {
  merchant: string;
  amount: number;     // positive number; sign is rendered from `type`, not from sign of `amount`
  type: 'income' | 'expense';
  category: TransactionCategory;  // from @/types/finance
  date: string;       // ISO 8601 date string; component formats with toLocaleDateString
}

export function TransactionItem(props: TransactionItemProps): JSX.Element;

<!-- Visual contract (08-UI-SPEC.md § Section 3): -->
<!--
  <div className="flex flex-col gap-bb-1 py-bb-3 border-b border-bb-border last:border-b-0">
    <div className="flex items-baseline justify-between">
      <span className="text-bb-base text-bb-text">{merchant}</span>
      <span className={cn("text-bb-base font-bold",
        type === 'income' ? 'text-bb-positive' : 'text-bb-negative')}>
        {prefix}{formatCurrency(amount)}
      </span>
    </div>
    <div className="flex items-center gap-bb-1">
      <span className="text-bb-sm text-bb-text-secondary">{category}</span>
      <span className="text-bb-sm text-bb-text-secondary">·</span>
      <span className="text-bb-sm text-bb-text-secondary">{formattedDate}</span>
    </div>
  </div>

  prefix: '+' for income, '−' (U+2212) for expense
  formattedDate: new Date(date).toLocaleDateString('de-DE', { day: 'numeric', month: 'short' })
-->
</interfaces>
</context>

<feature>
  <name>TransactionItem presentational server component</name>
  <files>
    src/components/dashboard/TransactionItem.tsx (CREATE),
    src/components/dashboard/index.ts (MODIFY — add re-export),
    tests/components/TransactionItem.test.tsx (MODIFY — replace it.todo with real it() cases)
  </files>
  <behavior>
    Render test cases (≥ 6 — use `render(<TransactionItem {...props} />)` from @testing-library/react and assert via `screen.getByText` + `container.querySelector` for class checks):

    1. EXPENSE_BASIC: `{ merchant: 'REWE Supermarkt', amount: 34.50, type: 'expense', category: 'Food', date: '2026-04-14' }` —
       - Asserts merchant text appears
       - Asserts amount element text starts with `−` (U+2212), NOT `-` (hyphen-minus U+002D)
       - Asserts the amount element has class `text-bb-negative` (querySelector `.text-bb-negative`)
       - Asserts the secondary line contains `Food` and the formatted date `14 Apr` (or whatever `toLocaleDateString('de-DE', {day:'numeric', month:'short'})` actually emits — verify with console once before pinning)
    2. INCOME_BASIC: same merchant but `type: 'income', category: 'Salary'` —
       - Asserts the amount element text starts with `+`
       - Asserts the amount element has class `text-bb-positive`
       - Asserts the secondary line contains `Salary`
    3. AMOUNT_FORMATTING: `amount: 1234.5` expense — assert the rendered string contains `1.234,50 €` (de-DE EUR, prefixed with U+2212)
    4. ZERO_AMOUNT: `amount: 0` — should render `+0,00 €` if `type === 'income'` and `−0,00 €` if `type === 'expense'`. (Edge case — confirms no special "is-zero" branch).
    5. CATEGORY_VARIETY: render with `category: 'Other'` — assert text "Other" appears in the secondary line. (Confirms the component accepts any TransactionCategory value.)
    6. DATE_FORMATTING: render with `date: '2026-04-14'` — assert formatted date matches `new Date('2026-04-14').toLocaleDateString('de-DE', { day: 'numeric', month: 'short' })` (compute the expected value in the test setup once so the assertion is exact).
    7. NO_ICONS: assert `container.querySelector('svg')` is null. (Per DESIGN_SYSTEM §5.3: "no icons".)
    8. NO_CLIENT_DIRECTIVE_REGRESSION_GUARD: read the source file content and assert the literal client-directive string does NOT appear in the first 200 characters. (Optional but recommended — this is a behavioral assertion that protects against future regression where someone adds the directive thoughtlessly.)

    Fixture: `const mockExpense: TransactionItemProps = { ... }` and `const mockIncome: TransactionItemProps = { ... }` declared at the top of the `describe` block. Mirror the fixture style in `tests/components/budget-progress.test.tsx`.
  </behavior>
  <implementation>
    File structure:

    1. FIRST LINE comment (NOT a directive — a regular line comment for future-reader guidance):
       Single-line `//` comment indicating the file is a server component and the client directive must not be added. No interactivity, no hooks.

    2. JSDoc file header (mirror SyncTransactionsButton header pattern; modify content):
       Header sections:
       - Title: "TransactionItem Component"
       - VIEW-MODEL DECOUPLING: accepts a view-model (TransactionItemProps), NOT a raw DbTransaction
       - VISUAL CONTRACT: primary row (merchant + amount), secondary row (category · date), income +/positive, expense −/negative, no icons, py-bb-3, last:border-b-0
       - DESIGN_SYSTEM §5.3 + 08-UI-SPEC.md § Section 3 — visual contract source
       - CONTEXT D-CMP-02 — view-model shape source
       - EXTENSION POINTS: future click-to-detail (wrap in Link); future per-transaction icons reserved column

    3. Section divider then imports:
       - `import { cn } from "@/lib/utils";`
       - `import { formatCurrency } from "@/utils/currency";`
       - `import type { TransactionCategory } from "@/types/finance";`

    4. Section divider "Types" then the `TransactionItemProps` interface — each prop documented with JSDoc `/** ... */`. Mirror the style in `SyncTransactionsButton.tsx` lines 32-37.

    5. Section divider "Component" then `export function TransactionItem({ merchant, amount, type, category, date }: TransactionItemProps)`.

    6. Inside the function body:
       - `const formattedDate = new Date(date).toLocaleDateString('de-DE', { day: 'numeric', month: 'short' });`
       - `const prefix = type === 'income' ? '+' : '−';` — NOTE: `−` is U+2212 MINUS SIGN, NOT hyphen-minus. Source it from RESEARCH.md Code Example 4. The editor must preserve it — confirm with `node -e "console.log(require('fs').readFileSync('src/components/dashboard/TransactionItem.tsx','utf8').includes('−'))"` printing `true` after writing.
       - `const amountClass = type === 'income' ? 'text-bb-positive' : 'text-bb-negative';`
       - Return the JSX from the `<interfaces>` block above. Use `cn("text-bb-base font-bold", amountClass)` for the amount span.

    7. NO default export. Named export only. Matches SyncTransactionsButton.

    8. Update `src/components/dashboard/index.ts` — add after the existing `export { SyncTransactionsButton } ...` block (alphabetical or end-of-list, executor's discretion):
       Re-export block with brief JSDoc:
       - Brief description ("Presentational single-row transaction display. Used on Home hub (Phase 8) and Transactions page (Phase 9).")
       - `@see docs/DESIGN_SYSTEM.md §5.3` and `@see .planning/phases/08-home-hub/08-CONTEXT.md D-CMP-02`
       - `export { TransactionItem, type TransactionItemProps } from "./TransactionItem";`

    9. Test file rewrite: replace `it.todo()` from Wave 0 with the 6-8 cases above. Use `render` + `screen` + `container` from @testing-library/react. Use `mockExpense` / `mockIncome` fixtures at the top of the describe block. For the U+2212 assertion, the test string literal must contain the actual U+2212 character; document with an inline comment so future readers don't normalize it to hyphen.
  </implementation>

  <read_first>
    - src/app/(bb)/page.tsx (lines 255-279) — the legacy inline JSX being extracted into this component; mirror layout structure but token-ize colors
    - src/app/(bb)/page.tsx (lines 316-321) — confirm formatCurrency behavior matches what `@/utils/currency` provides (08-03 just shipped this)
    - src/components/dashboard/SyncTransactionsButton.tsx (lines 1-37) — file-header + Props-interface placement pattern (interface defined immediately after imports, before component, with per-prop JSDoc)
    - src/components/dashboard/index.ts (entire file) — barrel re-export pattern; add new export in the same style
    - src/types/finance.ts (lines 14-45) — TransactionCategory union type (IncomeCategory | ExpenseCategory)
    - src/lib/utils.ts — confirm `cn` helper is exported as a named export
    - tests/components/PageShell.test.tsx (lines 1-60) — render-test boilerplate; `render(<X />)`, `screen.getByText`, `container.querySelector`
    - tests/components/budget-progress.test.tsx (lines 24-70) — fixture object pattern
    - .planning/phases/08-home-hub/08-CONTEXT.md (D-CMP-02) — view-model interface lock
    - .planning/phases/08-home-hub/08-UI-SPEC.md (§ "Section 3: Recent Transactions", § "TransactionItem visual contract") — full visual spec including U+2212 + U+00B7 + de-DE date format
    - .planning/phases/08-home-hub/08-RESEARCH.md (§ Code Examples Example 4, § Common Pitfalls 4 & 5) — reference component + booking_date pitfall + client-directive pitfall
    - .planning/phases/08-home-hub/08-PATTERNS.md (§ "src/components/dashboard/TransactionItem.tsx", § "Token-Only Styling", § "No client directive in New Server Components") — pattern map
  </read_first>

  <acceptance_criteria>
    - `src/components/dashboard/TransactionItem.tsx` exists, exports `TransactionItem` (function) AND `TransactionItemProps` (interface)
    - File does NOT contain the client-directive string at module top (regression guard test confirms)
    - File contains the U+2212 MINUS SIGN character `−` (not just hyphen): `node -e "console.log(require('fs').readFileSync('src/components/dashboard/TransactionItem.tsx','utf8').includes('−'))"` prints `true`
    - JSX uses ONLY `--bb-*` Tailwind utilities: no `text-green-*`, `text-red-*`, `text-gray-*`, or inline hex/rgb (`grep -E "text-(green|red|gray|blue|yellow)-" src/components/dashboard/TransactionItem.tsx | grep -v '//'` returns nothing)
    - File contains all required token classes: `text-bb-base`, `text-bb-sm`, `text-bb-text`, `text-bb-text-secondary`, `text-bb-positive`, `text-bb-negative`, `border-bb-border`, `font-bold` (grep each)
    - File imports `cn` from `@/lib/utils` and `formatCurrency` from `@/utils/currency`
    - `src/components/dashboard/index.ts` re-exports `TransactionItem` and the `TransactionItemProps` type
    - `tests/components/TransactionItem.test.tsx` contains ≥ 6 `it()` cases (no `it.todo` remaining)
    - `bun run test tests/components/TransactionItem.test.tsx` exits 0
    - `bun run typecheck` exits 0
    - 2 TDD commits on the branch (RED + GREEN)
  </acceptance_criteria>

  <verify>
    <automated>bun run test tests/components/TransactionItem.test.tsx --reporter=verbose 2>&1 | tail -30 && bun run typecheck 2>&1 | tail -5 && echo "--- token check ---" && grep -E "text-(green|red|gray|blue|yellow)-" src/components/dashboard/TransactionItem.tsx | grep -v '//' | wc -l</automated>
  </verify>

  <done>
    Component implemented, all render tests pass, U+2212 MINUS SIGN verified present, no hardcoded colors, no client directive, barrel re-export updated, RED + GREEN commits on branch.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view-model props | Component receives a view-model from the parent page; parent maps from RLS-filtered DB rows. No untrusted input crosses this component. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Client directive leaking in causes server page to fall back to client bundle (per RESEARCH Pitfall 5) | mitigate | First-line comment + regression guard test (case 8) explicitly checks the directive is absent. CI build catches client-bundle inflation. |
| T-08-04-02 | Information Disclosure | TransactionItem renders user transaction data (merchant, amount, category) — visible only to authenticated user via RLS-filtered query upstream | accept | The middleware + RLS combination guarantees the user can only see their own data. Component does not bypass this. |
| T-08-04-03 | Tampering | Future change replaces `−` with `-` (hyphen) — breaks semantic distinction between negative and minus | mitigate | Test case 1 asserts the U+2212 byte sequence. Source file comment documents the choice. |
| T-08-04-04 | Cross-Site Scripting | React's default JSX escaping handles merchant/category/date — no raw-HTML props are used in this component | accept | Standard React XSS prevention applies. No raw HTML insertion API is invoked. |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No installs. |
</threat_model>

<verification>
- `bun run test tests/components/TransactionItem.test.tsx` ≥ 6 passing tests
- `bun run typecheck` exits 0
- `grep -E "text-(green|red|gray|blue|yellow)-" src/components/dashboard/TransactionItem.tsx | grep -v '//' | wc -l` returns 0 (no hardcoded color utilities outside comments)
- U+2212 character present in source
- Client directive not present in source
- Barrel exports `TransactionItem`
</verification>

<success_criteria>
- TDD discipline observed
- Component visually matches UI-SPEC contract
- Server component (no client overhead)
- Decoupled from DB schema via view-model
- Ready for consumption by plan 08-05 (home page rewrite)
</success_criteria>

<output>
Create `.planning/phases/08-home-hub/08-04-SUMMARY.md` when done. Include commit SHAs (RED + GREEN), final test count, exact U+2212 verification result, and any deviation from UI-SPEC contract.
</output>
