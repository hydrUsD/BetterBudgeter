---
phase: 08-home-hub
plan: 02
type: tdd
wave: 1
depends_on: ["08-00"]
files_modified:
  - tests/utils/greeting.test.ts
  - src/utils/greeting.ts
autonomous: true
requirements: [PAGE-01]
tags: [phase-8, home-hub, greeting, tdd, pure-utility]

must_haves:
  truths:
    - "nameFromEmail extracts capitalized first name from typical emails and returns null for unparseable input"
    - "greetingForTime returns the correct band ('morning' | 'afternoon' | 'evening') across Europe/Berlin timezone boundaries"
    - "Both helpers are pure (no DOM, no Date.now(), no globals — all input via parameters)"
    - "All boundary test cases (D-GR-02 time band edges; D-GR-01 algorithm edges) pass"
  artifacts:
    - path: "src/utils/greeting.ts"
      provides: "Pure greeting helpers (PAGE-01 greeting)"
      exports: ["nameFromEmail", "greetingForTime"]
      contains: "export function nameFromEmail"
      min_lines: 30
    - path: "tests/utils/greeting.test.ts"
      provides: "Unit test coverage for both helpers"
      contains: "describe(\"greetingForTime\""
      min_lines: 80
  key_links:
    - from: "src/utils/greeting.ts"
      to: "Intl.DateTimeFormat with Europe/Berlin timezone"
      via: "runtime API"
      pattern: "timeZone:.*Europe/Berlin"
---

<objective>
Implement `src/utils/greeting.ts` with two pure helpers — `nameFromEmail(email)` and `greetingForTime(now)` — using strict TDD. Algorithms are locked by D-GR-01, D-GR-02, D-GR-03 in 08-CONTEXT.md. Both helpers are pure: they accept all inputs as parameters and have no side effects.

Purpose: PAGE-01 requires a "time-aware greeting" on the Home hub. Server-rendered (D-GR-04), so no hydration flash. Splitting into two named exports keeps each function single-responsibility and trivially testable.

Output:
- `tests/utils/greeting.test.ts` with at least 13 `it()` cases (7 greetingForTime boundaries + 6 nameFromEmail edge cases)
- `src/utils/greeting.ts` with both functions, docblock per CLAUDE.md, no external deps beyond `Intl.DateTimeFormat`
- 2 atomic commits (RED → GREEN)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/08-home-hub/08-CONTEXT.md
@.planning/phases/08-home-hub/08-RESEARCH.md
@.planning/phases/08-home-hub/08-PATTERNS.md
@src/utils/charts/index.ts

<interfaces>
<!-- Pure helpers — no external types. Public API (locked): -->

export function nameFromEmail(email: string | null | undefined): string | null;
export function greetingForTime(now: Date): 'morning' | 'afternoon' | 'evening';

<!-- nameFromEmail algorithm (D-GR-01):
     1. Reject non-string, null, undefined, empty → null
     2. Take portion before '@'; if empty → null
     3. Split on /[._-]/, take first chunk; if empty → null
     4. Reject if first char is not /[a-zA-Z]/ → null
     5. Return: first char uppercased + rest lowercased -->

<!-- greetingForTime bands (D-GR-02, D-GR-03):
     - 'morning':   05:00 ≤ hour < 12:00 Europe/Berlin
     - 'afternoon': 12:00 ≤ hour < 18:00 Europe/Berlin
     - 'evening':   else (18:00–23:59, 00:00–04:59 Europe/Berlin) -->
</interfaces>
</context>

<feature>
  <name>Pure greeting helpers</name>
  <files>
    src/utils/greeting.ts (CREATE),
    tests/utils/greeting.test.ts (MODIFY — replace it.todo with real it() cases)
  </files>
  <behavior>
    `nameFromEmail` test cases (≥ 6):
    1. `nameFromEmail('paul.heuwer@example.com')` → `'Paul'` (dot separator, capitalization)
    2. `nameFromEmail('paul_h@example.com')` → `'Paul'` (underscore separator)
    3. `nameFromEmail('paul-heuwer@example.com')` → `'Paul'` (dash separator)
    4. `nameFromEmail('PAUL@example.com')` → `'Paul'` (already uppercase — must normalize: first char uppercase + rest LOWERCASE)
    5. `nameFromEmail('a@example.com')` → `'A'` (single character)
    6. `nameFromEmail('123abc@example.com')` → `null` (numeric prefix rejected)
    7. `nameFromEmail(null)` → `null`
    8. `nameFromEmail(undefined)` → `null`
    9. `nameFromEmail('')` → `null`
    10. `nameFromEmail('@example.com')` → `null` (empty local part)

    `greetingForTime` test cases (≥ 7 — covers every band-boundary; uses fixed ISO UTC dates compensating for CET/CEST offset):
    1. 04:59 Berlin → `'evening'` — winter date `new Date('2026-01-15T03:59:00Z')` (CET = UTC+1, so 03:59Z = 04:59 Berlin)
    2. 05:00 Berlin → `'morning'` — `new Date('2026-01-15T04:00:00Z')`
    3. 11:59 Berlin → `'morning'` — `new Date('2026-01-15T10:59:00Z')`
    4. 12:00 Berlin → `'afternoon'` — `new Date('2026-01-15T11:00:00Z')`
    5. 17:59 Berlin → `'afternoon'` — `new Date('2026-01-15T16:59:00Z')`
    6. 18:00 Berlin → `'evening'` — `new Date('2026-01-15T17:00:00Z')`
    7. 00:00 Berlin → `'evening'` — `new Date('2026-01-14T23:00:00Z')` (wraps midnight)
    8. Summer DST: 18:00 Berlin CEST → `'evening'` — `new Date('2026-07-15T16:00:00Z')` (CEST = UTC+2; CONFIRMS the function uses the live Europe/Berlin zone, not a hardcoded +1 offset)

    Each test case uses an explicit comment showing the Berlin local time being tested so future readers (junior devs) understand the UTC arithmetic.
  </behavior>
  <implementation>
    File structure (mirror `src/utils/charts/index.ts` header pattern):

    1. JSDoc file header — title "Greeting Helpers" — explains:
       - WHAT: pure helpers for the Home hub greeting line
       - WHY pure: server-side computed per request (D-GR-04); each function is parameter-only so unit tests don't need fake timers
       - SCOPE: English-only, Europe/Berlin timezone hardcoded — full i18n is a future milestone (CONTEXT D-GR-02, D-GR-03)
       - "No side effects. No DOM access. No external state."

    2. Section divider "nameFromEmail" then the function:
       - Signature: `export function nameFromEmail(email: string | null | undefined): string | null`
       - JSDoc with algorithm steps 1-5 (paste from 08-RESEARCH.md Example 2 docblock)
       - Implementation: explicit guards, then `email.split('@')[0]`, then `localPart.split(/[._-]/)[0]`, then `/^[a-zA-Z]/.test(firstChunk)`, then return `firstChunk.charAt(0).toUpperCase() + firstChunk.slice(1).toLowerCase()`
       - Add a TODO comment: `// TODO (future): swap for AuthUser.display_name when bb_user_settings supports it. Same call site, replace this import.`

    3. Section divider "greetingForTime" then the function:
       - Signature: `export function greetingForTime(now: Date): 'morning' | 'afternoon' | 'evening'`
       - JSDoc with the three bands + "Timezone: hardcoded Europe/Berlin (D-GR-03)"
       - Use the ROBUST `Intl.DateTimeFormat(...).formatToParts()` pattern from 08-RESEARCH.md Example 3, NOT `toLocaleString(...)`. Rationale (commented inline): `// Using formatToParts() instead of toLocaleString() because the string format of toLocaleString('en-US', {hour: 'numeric', hour12: false}) varies between Node.js versions (some return "17", some "17:00"). formatToParts() unambiguously returns a 'hour' part as a numeric string. See RESEARCH §Pitfall 3.`
       - Fallback for unknown part value: `parts.find(p => p.type === 'hour')?.value ?? '12'` then `parseInt(..., 10)` then explicit `if (hour >= 5 && hour < 12) return 'morning';` / `if (hour >= 12 && hour < 18) return 'afternoon';` / `return 'evening';`
       - TODO comment: `// TODO (future i18n milestone): accept timezone parameter; read user-preferred locale from bb_user_settings.`

    4. NO default export. Both functions named-exported. Matches the `src/utils/` convention.

    No `"use client"`. No React. No Node-only APIs (`Intl` is universal). Module works in both server and client contexts (though Phase 8 only uses it server-side).

    Test file rewrite — replace it.todo() with the 13+ real cases above. For each greetingForTime case, include an inline comment showing the Berlin local time being tested. Group into `describe("nameFromEmail", ...)` and `describe("greetingForTime", ...)`. No `beforeAll` / `afterAll` (per `tests/setup.ts` global cleanup).
  </implementation>

  <read_first>
    - src/utils/charts/index.ts (lines 1-8) — file-header pattern + "no side effects" statement
    - tests/utils/charts.test.ts (lines 1-37) — test structure (describe + it + section dividers)
    - .planning/phases/08-home-hub/08-CONTEXT.md (D-GR-01..04) — locked algorithm and time bands
    - .planning/phases/08-home-hub/08-RESEARCH.md (§ Code Examples Examples 2 & 3, § Common Pitfalls 3 & 6, § Validation Architecture greetingForTime test list) — reference implementation + pitfalls + Nyquist test list
    - .planning/phases/08-home-hub/08-PATTERNS.md (§ "src/utils/greeting.ts" entry) — pattern map; particularly the test pattern for timezone-dependent functions
  </read_first>

  <acceptance_criteria>
    - `src/utils/greeting.ts` exports exactly two named functions: `nameFromEmail` and `greetingForTime`
    - `nameFromEmail` parameter type is `string | null | undefined` (NOT just `string`)
    - `greetingForTime` uses `Intl.DateTimeFormat(...).formatToParts(...)` (grep: `formatToParts`) — NOT `toLocaleString`
    - The timezone string `'Europe/Berlin'` appears in `greetingForTime`'s implementation
    - `tests/utils/greeting.test.ts` contains at least 13 `it()` cases across two `describe` blocks
    - `bun run test tests/utils/greeting.test.ts` exits 0
    - `bun run typecheck` exits 0
    - Each `greetingForTime` test case has an inline comment showing the Berlin local time being tested (junior-dev clarity)
    - No `"use client"` directive
    - No React, no DOM, no `Date.now()` (now is parameter)
    - 2 TDD commits on the branch (RED + GREEN)
  </acceptance_criteria>

  <verify>
    <automated>bun run test tests/utils/greeting.test.ts --reporter=verbose 2>&1 | tail -40 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>

  <done>
    All 13+ test cases pass; both functions are pure (no closures over module state, no globals); `greetingForTime` uses formatToParts (not toLocaleString); RED + GREEN commits exist.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email input | `nameFromEmail` accepts an email string from `getUser()`. Supabase validates this upstream; the helper does not trust the format. |
| Date input | `greetingForTime` accepts a Date object; caller passes `new Date()` per-request. No external date sourcing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | Email displayed via `nameFromEmail` could expose part of the user's email to anyone shoulder-surfing | mitigate | nameFromEmail strips the domain and extracts only the local-part first chunk. The greeting shows "Paul" not "paul.heuwer@example.com". |
| T-08-02-02 | Denial of Service | Malformed email causing the regex `[._-]` split to throw or hang | mitigate | nameFromEmail uses no `match()` with backtracking — only `.split()` with character class — and has explicit null guards. Unit tests cover empty string and `'@example.com'`. |
| T-08-02-03 | Spoofing | `greetingForTime` returns wrong band → user sees "Good evening" at 9am (cosmetic) | accept | Cosmetic-only impact. Comprehensive boundary tests (7 cases) detect regressions. No security impact. |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No installs. |
</threat_model>

<verification>
- `bun run test tests/utils/greeting.test.ts` ≥ 13 passing tests
- `bun run typecheck` exits 0
- Source uses `Intl.DateTimeFormat(...).formatToParts(...)` not `toLocaleString`
- Source uses `'Europe/Berlin'` literal
- nameFromEmail handles null/undefined/empty without throwing
</verification>

<success_criteria>
- TDD discipline observed (RED → GREEN commits)
- All 13+ Nyquist cases pass
- Module is pure and importable from both server and client (no `"use client"` declaration needed; no Node-only APIs used)
- Future readers understand the timezone arithmetic from comments alone
</success_criteria>

<output>
Create `.planning/phases/08-home-hub/08-02-SUMMARY.md` when done. Include commit SHAs (RED + GREEN), final test count, and a note if any test boundary required adjustment due to runtime behavior.
</output>
