---
phase: 08-home-hub
plan: 03
type: tdd
wave: 1
depends_on: ["08-00"]
files_modified:
  - tests/utils/currency.test.ts
  - src/utils/currency.ts
autonomous: true
requirements: [PAGE-03]
tags: [phase-8, home-hub, currency, tdd, pure-utility, extraction]

must_haves:
  truths:
    - "formatCurrency returns the same string the legacy inline helper produced (de-DE EUR)"
    - "formatCurrency is the single source of truth for EUR formatting in BB code"
    - "Both the home page and TransactionItem can import from a single location"
  artifacts:
    - path: "src/utils/currency.ts"
      provides: "Pure EUR currency formatter (extracted from legacy inline)"
      exports: ["formatCurrency"]
      contains: "Intl.NumberFormat"
      min_lines: 15
    - path: "tests/utils/currency.test.ts"
      provides: "Unit test coverage for formatCurrency"
      contains: "describe(\"formatCurrency\""
      min_lines: 30
  key_links:
    - from: "src/utils/currency.ts"
      to: "Intl.NumberFormat with de-DE locale and EUR currency"
      via: "runtime API"
      pattern: "Intl\\.NumberFormat.*de-DE.*EUR"
---

<objective>
Extract the inline `formatCurrency` helper from the legacy `src/app/(bb)/page.tsx` (lines 316-321) into a new flat-file module `src/utils/currency.ts` so both the home page and `TransactionItem` can import it from a single location. Behavior is unchanged: `new Intl.NumberFormat("de-DE", { style: "currency", currency: "EUR" }).format(amount)`. Per D-CUT-04.

Filename decision: `currency.ts` (not `format.ts`) per 08-RESEARCH.md finding 6 / 08-PATTERNS.md naming conflict warning. The legacy `src/utils/format/index.ts` (USD-defaulting) exists; creating a flat `src/utils/format.ts` alongside that directory would create TypeScript module-resolution ambiguity. Naming the new file `currency.ts` is semantically more accurate AND avoids the conflict entirely.

Purpose: D-CUT-04 establishes single source of truth for EUR formatting. PAGE-03's TransactionItem needs it; the home page hero/budget rows need it.

Output:
- `tests/utils/currency.test.ts` with ≥ 4 cases
- `src/utils/currency.ts` with one named export `formatCurrency`
- 2 TDD commits (RED → GREEN)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/08-home-hub/08-CONTEXT.md
@.planning/phases/08-home-hub/08-RESEARCH.md
@.planning/phases/08-home-hub/08-PATTERNS.md
@src/utils/format/index.ts
@src/app/(bb)/page.tsx

<interfaces>
<!-- Public API (locked): -->
export function formatCurrency(amount: number): string;

<!-- Behavior: equivalent to
     new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(amount) -->

<!-- Naming conflict avoided:
     - Legacy: src/utils/format/index.ts → exports formatCurrency(amount, currency?, locale?) defaulting USD
     - New: src/utils/currency.ts → exports formatCurrency(amount: number) locked de-DE/EUR
     Import paths differ (@/utils/format vs @/utils/currency) — no ambiguity. -->

<!-- Note for CONTEXT.md update needed:
     D-CMP-01 in CONTEXT.md says "src/utils/format.ts". The implementation deviates to
     "src/utils/currency.ts" per 08-RESEARCH.md finding 6 and 08-PATTERNS.md naming-
     conflict warning. This is a planning-time decision, NOT a scope reduction —
     the same function with the same behavior, in a better-named file. Note this
     deviation in the plan summary. -->
</interfaces>
</context>

<feature>
  <name>EUR currency formatter — extraction from legacy inline helper</name>
  <files>
    src/utils/currency.ts (CREATE),
    tests/utils/currency.test.ts (MODIFY — replace it.todo with real it() cases)
  </files>
  <behavior>
    `formatCurrency` test cases (≥ 4):
    1. `formatCurrency(0)` → `"0,00 €"` (zero with German decimal comma)
    2. `formatCurrency(1234.5)` → `"1.234,50 €"` (German thousand separator dot + decimal comma)
    3. `formatCurrency(10000)` → `"10.000,00 €"` (large positive with thousand separator)
    4. `formatCurrency(-50.5)` → `"-50,50 €"` (negative; NOT the U+2212 character — Intl uses hyphen-minus; the TransactionItem applies U+2212 outside formatCurrency)
    5. (Optional but recommended) `formatCurrency(0.05)` → `"0,05 €"` (fractional below 1)

    IMPORTANT — Intl.NumberFormat output format note: Different Node.js / ICU versions render the currency symbol either as `1.234,50 €` (with U+00A0 NO-BREAK SPACE) or `1.234,50 €` (regular space). When writing the assertion strings, copy the EXACT output from a `console.log` in the project's bun runtime — DO NOT eyeball the value. The test file MUST include a comment near the first assertion: `// NOTE: The space between number and "€" is U+00A0 (NO-BREAK SPACE) as emitted by ICU. Use   in test strings or copy the raw output from the test command.`

    No de-en mismatch test needed — D-CUT-04 locks behavior to de-DE/EUR.
  </behavior>
  <implementation>
    File structure (mirror `src/utils/format/index.ts` legacy file's header pattern but narrowed):

    1. JSDoc file header:
       ```
       /**
        * Currency Formatter (EUR, de-DE)
        *
        * Single source of truth for EUR currency formatting across BetterBudgeter.
        * Extracted from the inline helper formerly in src/app/(bb)/page.tsx (D-CUT-04).
        *
        * Pure function — no side effects, no DOM access, no external state.
        *
        * Why a new flat file (and not `format.ts` per CONTEXT D-CMP-01):
        *   The legacy `src/utils/format/index.ts` exists with a 3-param formatCurrency
        *   (USD-defaulting). To avoid TypeScript module-resolution ambiguity and to
        *   give this function a semantically accurate name, the planner renamed it
        *   to `currency.ts`. See 08-PATTERNS.md naming-conflict warning.
        *
        * EXTENSION POINT: When multi-currency support is added (v3+),
        *   add a currency parameter and migrate callers gradually. Until then,
        *   this function is locked to de-DE/EUR.
        */
       ```

    2. Section divider then:
       ```ts
       export function formatCurrency(amount: number): string {
         return new Intl.NumberFormat('de-DE', {
           style: 'currency',
           currency: 'EUR',
         }).format(amount);
       }
       ```

    The function body MUST be byte-identical (modulo whitespace) to the legacy inline helper in (bb)/page.tsx:316-321 — this is an extraction, not a rewrite.

    No imports. No types beyond `(amount: number)`. No `"use client"`.

    Test file rewrite — replace `it.todo()` with ≥ 4 real cases. Single `describe("formatCurrency", ...)` block. Before writing assertion strings, executor runs once: `node -e "console.log(JSON.stringify(new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR'}).format(1234.5)))"` to confirm the exact byte sequence (U+00A0 vs space), then encodes it correctly in the assertion (`"1.234,50 €"` if the runtime emits NO-BREAK SPACE).
  </implementation>

  <read_first>
    - src/utils/format/index.ts (lines 1-37 if shorter) — legacy formatCurrency with USD default; understand the existing API to ensure no conflict
    - src/app/(bb)/page.tsx (lines 313-321) — the exact inline helper being extracted; byte-preserve the body
    - tests/utils/charts.test.ts (lines 1-37) — test file structure
    - .planning/phases/08-home-hub/08-CONTEXT.md (D-CUT-04) — locked behavior
    - .planning/phases/08-home-hub/08-RESEARCH.md (§ Code Examples Example 5, finding 6 in critical research findings — Option A) — implementation reference + filename decision rationale
    - .planning/phases/08-home-hub/08-PATTERNS.md (§ "src/utils/format.ts" entry, § "Naming Conflict Warning") — pattern + the explicit currency.ts fallback recommendation
  </read_first>

  <acceptance_criteria>
    - `src/utils/currency.ts` exists at that exact path (NOT `src/utils/format.ts`)
    - File exports exactly one named function: `formatCurrency(amount: number): string`
    - Function body contains `new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(amount)` (single quotes or double quotes both OK)
    - `tests/utils/currency.test.ts` contains ≥ 4 `it()` cases inside `describe("formatCurrency", ...)`
    - Test file contains the inline NOTE comment about U+00A0 NO-BREAK SPACE
    - Assertion strings use the actual runtime output (U+00A0 if emitted) — NOT a guessed value
    - `bun run test tests/utils/currency.test.ts` exits 0
    - `bun run typecheck` exits 0 (proves the new file does NOT collide with `src/utils/format/index.ts` in module resolution)
    - No `"use client"`; no imports
    - 2 TDD commits on branch
  </acceptance_criteria>

  <verify>
    <automated>bun run test tests/utils/currency.test.ts --reporter=verbose 2>&1 | tail -20 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>

  <done>
    Currency formatter extracted, tested, and importable from `@/utils/currency`; legacy inline helper still in legacy (bb)/page.tsx for now (that file is rewritten in plan 08-05, which removes the inline helper entirely); typecheck clean.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| number input | `formatCurrency` accepts a number from `BudgetProgress.remainingAmount` or `DbTransaction.amount`. Source data already RLS-filtered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | Locale change in future could subtly break formatting (e.g. someone passes `en-US` defaulting) | mitigate | Function signature accepts no locale param — hardcoded de-DE/EUR. EXTENSION POINT comment documents the v3+ migration path. |
| T-08-03-02 | Information Disclosure | None — function only formats numbers, no PII flows through | accept | Low risk. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | accept | No installs. |
</threat_model>

<verification>
- `bun run test tests/utils/currency.test.ts` ≥ 4 passing tests
- `bun run typecheck` exits 0 (proves no naming collision)
- Reading `src/utils/currency.ts`, the formatter body matches legacy line-for-line (modulo whitespace)
- Inline U+00A0 NOTE comment present in test file
</verification>

<success_criteria>
- TDD discipline observed
- Single source of truth for EUR formatting now exists
- Future plans (08-04 TransactionItem, 08-05 page rewrite) import from `@/utils/currency`
- Filename deviation from CONTEXT D-CMP-01 documented in plan summary
</success_criteria>

<output>
Create `.planning/phases/08-home-hub/08-03-SUMMARY.md` when done. Explicitly note the filename change `format.ts` → `currency.ts` so plan-checker and downstream plans know.
</output>
