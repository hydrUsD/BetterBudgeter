---
phase: 08-home-hub
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/lib/safe-to-spend.test.ts
  - tests/utils/greeting.test.ts
  - tests/utils/currency.test.ts
  - tests/components/TransactionItem.test.tsx
autonomous: true
requirements: [PAGE-01, PAGE-03, PAGE-08]
tags: [phase-8, home-hub, test-scaffolding, wave-0, tdd]

must_haves:
  truths:
    - "All four Phase 8 test files exist with valid imports and an it.todo stub each"
    - "Running `bun run test` from the repo root completes without import errors"
    - "The tests/lib/ directory exists"
  artifacts:
    - path: "tests/lib/safe-to-spend.test.ts"
      provides: "Test scaffold for computeSafeToSpend (PAGE-08)"
      contains: "describe(\"computeSafeToSpend\""
    - path: "tests/utils/greeting.test.ts"
      provides: "Test scaffold for nameFromEmail + greetingForTime (PAGE-01 greeting)"
      contains: "describe(\"nameFromEmail\""
    - path: "tests/utils/currency.test.ts"
      provides: "Test scaffold for formatCurrency (D-CUT-04)"
      contains: "describe(\"formatCurrency\""
    - path: "tests/components/TransactionItem.test.tsx"
      provides: "Render test scaffold for TransactionItem (PAGE-03)"
      contains: "describe(\"TransactionItem\""
  key_links:
    - from: "tests/lib/safe-to-spend.test.ts"
      to: "@/lib/safe-to-spend"
      via: "import statement"
      pattern: "from \"@/lib/safe-to-spend\""
    - from: "tests/utils/greeting.test.ts"
      to: "@/utils/greeting"
      via: "import statement"
      pattern: "from \"@/utils/greeting\""
    - from: "tests/utils/currency.test.ts"
      to: "@/utils/currency"
      via: "import statement"
      pattern: "from \"@/utils/currency\""
    - from: "tests/components/TransactionItem.test.tsx"
      to: "@/components/dashboard/TransactionItem"
      via: "import statement"
      pattern: "from \"@/components/dashboard/TransactionItem\""
---

<objective>
Create empty-but-importable test files for every Phase 8 testable module so Wave 1 can begin in TDD (RED) state without "module not found" import failures blocking the cycle. Also create the `tests/lib/` subdirectory (does not exist yet).

Purpose: TDD gate is enforced (`tdd_mode: true` in config.json). Wave 1's RED-step requires test files that exist and import the not-yet-implemented module path. This plan delivers the scaffolds in one atomic commit.

Output: 4 test files with proper docblock, vitest imports, and a single `it.todo()` placeholder per `describe` block. No assertions — Wave 1's TDD tasks add the real `it()` cases before implementing the modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-home-hub/08-CONTEXT.md
@.planning/phases/08-home-hub/08-RESEARCH.md
@.planning/phases/08-home-hub/08-PATTERNS.md
@.planning/phases/08-home-hub/08-VALIDATION.md
@tests/setup.ts
@tests/utils/charts.test.ts
@tests/components/PageShell.test.tsx
@vitest.config.ts

<interfaces>
<!-- Modules that DO NOT exist yet — these tests stub their import paths so the
     TDD cycle in Wave 1 sees "module not found" → adds test cases (RED) →
     implements module (GREEN). Wave 0 only stubs the file shells. -->

Future module: @/lib/safe-to-spend
  exports: ESSENTIAL_CATEGORIES, computeSafeToSpend(accounts, budgetProgress)

Future module: @/utils/greeting
  exports: nameFromEmail(email), greetingForTime(now)

Future module: @/utils/currency
  exports: formatCurrency(amount)

Future module: @/components/dashboard/TransactionItem
  exports: TransactionItem (component), TransactionItemProps (type)

Existing test infrastructure (no changes):
  - tests/setup.ts — vitest globals + jest-dom + ResizeObserver stub already wired
  - vitest.config.ts — `include: ["tests/**/*.test.{ts,tsx}"]`, `environment: "jsdom"`
  - No vi.mock() needed for these pure-function tests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/lib/ directory and four Phase 8 test scaffolds</name>

  <files>
    tests/lib/safe-to-spend.test.ts (CREATE),
    tests/utils/greeting.test.ts (CREATE),
    tests/utils/currency.test.ts (CREATE),
    tests/components/TransactionItem.test.tsx (CREATE)
  </files>

  <read_first>
    - tests/utils/charts.test.ts (lines 1-37) — exact docblock + import + describe structure to mirror for all 4 files
    - tests/components/PageShell.test.tsx (lines 1-60) — exact React Testing Library + render() + screen pattern for TransactionItem.test.tsx
    - tests/setup.ts — confirm what's already globally available (jest-dom matchers, cleanup, ResizeObserver) so the new files do NOT duplicate setup
    - vitest.config.ts — confirm `include` pattern matches `tests/**/*.test.{ts,tsx}`
    - .planning/phases/08-home-hub/08-RESEARCH.md (§ Validation Architecture, "Phase Requirements → Test Map") — full per-module test case list (informational; Wave 1 will write the real `it()` cases)
    - .planning/phases/08-home-hub/08-PATTERNS.md (§ "Test File Boilerplate", § "tests/setup.ts is already configured") — the required "Does NOT test:" docblock section and the no-per-file-setup rule
  </read_first>

  <action>
    Create four files, each following the exact `tests/utils/charts.test.ts` boilerplate pattern. Use bun's directory-creation-on-write or run `mkdir -p tests/lib` before writing the first file — `tests/utils/` and `tests/components/` already exist, but `tests/lib/` does not.

    File 1: `tests/lib/safe-to-spend.test.ts`
    - Docblock title: "computeSafeToSpend Unit Tests"
    - Docblock body: one sentence — "Tests the pure Safe-to-Spend calculation function (PAGE-08)."
    - Required "Does NOT test:" section listing: "DB orchestration (handled by the page component); UI rendering of the hero (handled by page-level smoke); the unused `getSafeToSpend` orchestrator wrapper if added later."
    - `@see docs/TESTING_STRATEGY.md`
    - Imports: `import { describe, it, expect } from "vitest";` then `import { computeSafeToSpend, ESSENTIAL_CATEGORIES } from "@/lib/safe-to-spend";` then `import type { DbAccount } from "@/lib/db/types";` then `import type { BudgetProgress } from "@/types/finance";`
    - One section divider then `describe("computeSafeToSpend", () => { it.todo("Wave 1 will populate the 7 cases from 08-RESEARCH.md § Nyquist Sampling Rationale"); });`
    - Second section divider then `describe("ESSENTIAL_CATEGORIES", () => { it.todo("Wave 1 will assert exact contents and as-const-ness"); });`

    File 2: `tests/utils/greeting.test.ts`
    - Docblock title: "greeting Helper Unit Tests"
    - Docblock body: "Tests pure helpers nameFromEmail and greetingForTime (PAGE-01 greeting; CONTEXT D-GR-01..04)."
    - "Does NOT test:" — "the page-level integration that combines them; React rendering of the greeting line."
    - Imports: `import { describe, it, expect } from "vitest";` then `import { nameFromEmail, greetingForTime } from "@/utils/greeting";`
    - Two `describe` blocks, one per exported function, each with one `it.todo()` referencing Wave 1.

    File 3: `tests/utils/currency.test.ts`
    - Docblock title: "formatCurrency Unit Tests"
    - Docblock body: "Tests the EUR currency formatter extracted from the legacy (bb)/page.tsx inline helper (D-CUT-04)."
    - "Does NOT test:" — "the legacy `@/utils/format/index.ts` formatter (USD-defaulting) — that file is unchanged; multi-currency variants (out of scope, v3+)."
    - Imports: `import { describe, it, expect } from "vitest";` then `import { formatCurrency } from "@/utils/currency";`
    - One `describe("formatCurrency", () => { it.todo("Wave 1 will populate the 4 cases from 08-RESEARCH.md (zero, positive, large, fractional)"); });`

    File 4: `tests/components/TransactionItem.test.tsx`
    - Docblock title: "TransactionItem Render Tests"
    - Docblock body: "Tests the presentational TransactionItem component (PAGE-03; CONTEXT D-CMP-02)."
    - "Does NOT test:" — "visual appearance or pixel measurements (covered by manual browser smoke); the parent page's DB-row → view-model mapping (covered in (bb)/page.tsx rewrite plan)."
    - Imports: `import { describe, it, expect } from "vitest";` then `import { render, screen } from "@testing-library/react";` then `import { TransactionItem, type TransactionItemProps } from "@/components/dashboard/TransactionItem";`
    - One `describe("TransactionItem", () => { it.todo("Wave 2 will populate income/expense/category/date render cases per 08-RESEARCH.md"); });`

    Every file uses the U+2500 (`─`) 80-char section dividers per 08-PATTERNS.md "Section Dividers". No `vi.mock()` calls — none of the modules under test have side-effectful imports. No `beforeAll` / `afterAll` — `tests/setup.ts` handles cleanup globally.

    IMPORTANT: at this stage all 4 modules being imported DO NOT EXIST. The `it.todo()` placeholders mean test runs succeed (todo is "skipped, planned"). But `tsc` (typecheck) WILL fail because the import paths don't resolve. This is expected and acceptable for the Wave 0 commit — typecheck is verified at Wave 4. The verify command for Wave 0 is `bun run test` (vitest with `it.todo()` is green even with missing module imports, because vitest defers module resolution to test execution — but if vitest errors on collection, the workaround is to add the empty module shell files in this same Wave 0 task; do NOT add module implementations).

    If `bun run test` errors with "Cannot find module" at collection time, create empty stub files immediately to unblock: `src/lib/safe-to-spend.ts` exporting `export {}`, `src/utils/greeting.ts` exporting `export {}`, `src/utils/currency.ts` exporting `export {}`, `src/components/dashboard/TransactionItem.tsx` containing `// @ts-expect-error placeholder` + `export const TransactionItem: any = () => null; export type TransactionItemProps = any;`. Note this fallback in the commit message so Wave 1 knows to fully replace the stubs.
  </read_first>

  <acceptance_criteria>
    - The four files exist at the exact paths listed in `<files>`
    - `tests/lib/` directory exists (was created)
    - Each test file's docblock contains a "Does NOT test:" section
    - Each test file has at least one `it.todo()` placeholder
    - Each test file imports from `vitest`
    - `tests/components/TransactionItem.test.tsx` imports `render` and `screen` from `@testing-library/react`
    - Running `bun run test` exits 0 (todos are skipped; if collection fails on missing modules, the fallback stub-files clause was applied)
    - No file contains a hardcoded color value or any production code beyond stub shims
  </acceptance_criteria>

  <verify>
    <automated>bun run test 2>&1 | tail -20 ; echo "---" ; ls tests/lib/safe-to-spend.test.ts tests/utils/greeting.test.ts tests/utils/currency.test.ts tests/components/TransactionItem.test.tsx</automated>
  </verify>

  <done>
    All 4 test files exist with correct imports + docblock + `it.todo()` placeholders. `bun run test` exits 0. `tests/lib/` directory exists. Wave 1 can now proceed to add real `it()` cases (RED) for each module.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem write | Test scaffolds and (possibly) stub source files are created. No runtime trust boundary crossed — tests run only in dev/CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | Stub source files (if fallback clause applied) accidentally shipped to production | mitigate | Stub source files (if needed) export `export {}` or `as any` placeholders — Wave 1 replaces them in the same phase before merge. Build gate at Wave 4 catches any leftover `any` placeholder. |
| T-08-00-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; existing vitest + @testing-library/react are already vetted per 08-RESEARCH.md § Package Legitimacy Audit. |
</threat_model>

<verification>
- Each test file exists, imports cleanly from `vitest` + `@testing-library/react`, contains `it.todo()` placeholder
- `bun run test` exits 0 (todos count as planned, not failures)
- `ls tests/lib/` confirms directory exists
- No production code added (only test scaffolds; stub source files allowed only as collection-time unblock per fallback clause)
</verification>

<success_criteria>
- 4 test files created, all importing from the future Phase 8 modules
- `bun run test` is green (todos OK, no real assertions yet)
- Wave 1 can begin TDD RED step without scaffolding overhead
</success_criteria>

<output>
Create `.planning/phases/08-home-hub/08-00-SUMMARY.md` when done. Summary should list which test files were created and whether the stub-source-file fallback was applied (Wave 1 must know to fully replace any stubs).
</output>
