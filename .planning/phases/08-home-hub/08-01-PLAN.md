---
phase: 08-home-hub
plan: 01
type: tdd
wave: 1
depends_on: ["08-00"]
files_modified:
  - tests/lib/safe-to-spend.test.ts
  - src/lib/safe-to-spend.ts
autonomous: true
requirements: [PAGE-08]
tags: [phase-8, home-hub, safe-to-spend, tdd, business-logic]

must_haves:
  truths:
    - "computeSafeToSpend returns a non-negative number for any valid input"
    - "computeSafeToSpend only counts checking + savings account balances (excludes credit, investment)"
    - "computeSafeToSpend only subtracts essential-category budget remainingAmount (Rent, Utilities, Food, Transport)"
    - "All 7 Nyquist test cases from 08-RESEARCH.md pass"
    - "Module exports ESSENTIAL_CATEGORIES as a readonly tuple via `as const`"
  artifacts:
    - path: "src/lib/safe-to-spend.ts"
      provides: "Pure Safe-to-Spend calculation (PAGE-08)"
      exports: ["ESSENTIAL_CATEGORIES", "computeSafeToSpend"]
      contains: "export function computeSafeToSpend"
      min_lines: 40
    - path: "tests/lib/safe-to-spend.test.ts"
      provides: "Unit test coverage for the formula"
      contains: "describe(\"computeSafeToSpend\""
      min_lines: 80
  key_links:
    - from: "src/lib/safe-to-spend.ts"
      to: "@/lib/db/types (DbAccount)"
      via: "import type"
      pattern: "import type \\{ DbAccount \\}"
    - from: "src/lib/safe-to-spend.ts"
      to: "@/types/finance (BudgetProgress)"
      via: "import type"
      pattern: "import type \\{ BudgetProgress \\}"
---

<objective>
Implement `src/lib/safe-to-spend.ts` — the pure calculation that powers the Home hub's hero metric — using strict TDD (RED → GREEN → REFACTOR). The formula is locked by D-S2S-01, D-S2S-02, D-S2S-03 in 08-CONTEXT.md. Implementation must be a pure function consuming `DbAccount[]` and `BudgetProgress[]` so it is unit-testable without any DB mock.

Purpose: PAGE-08 requires `lib/safe-to-spend.ts` with formula comments and limitations documented. The pure two-layer split (per 08-PATTERNS.md "Two-layer function design") guarantees the formula is verifiable in isolation; the page integrates it.

Output:
- `tests/lib/safe-to-spend.test.ts` with at least 7 `it()` cases covering the Nyquist axes
- `src/lib/safe-to-spend.ts` with `ESSENTIAL_CATEGORIES` const, `computeSafeToSpend()` function, multi-paragraph file-header explaining formula + WHY + v3+ TODO
- 2 atomic commits: `test(08-01): RED — safe-to-spend cases failing` then `feat(08-01): GREEN — implement computeSafeToSpend per D-S2S-01..03`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/08-home-hub/08-CONTEXT.md
@.planning/phases/08-home-hub/08-RESEARCH.md
@.planning/phases/08-home-hub/08-PATTERNS.md
@src/lib/budgets/index.ts
@src/lib/db/types.ts
@src/types/finance.ts

<interfaces>
<!-- Existing types this module consumes (do NOT modify these files in this plan). -->

From src/lib/db/types.ts (lines 19-31):
```ts
export interface DbAccount {
  id: string;
  user_id: string;
  bank_id: string;
  bank_name: string;
  account_name: string;
  account_type: string;     // ← snake_case, plain string (NOT typed AccountType enum)
  currency: string;
  balance: number;
  iban: string | null;
  last_synced_at: string | null;
  created_at: string;
}
```

From src/types/finance.ts:
```ts
export type ExpenseCategory =
  | "Food" | "Rent" | "Utilities" | "Transport"
  | "Entertainment" | "Shopping" | "Other";

export interface BudgetProgress {
  budget: { id: string; category: ExpenseCategory; monthlyLimit: number; /* ...other fields... */ };
  spentAmount: number;
  remainingAmount: number;       // ← Already Math.max(0, ...) clamped (src/lib/budgets/index.ts:174)
  usagePercentage: number;
  status: "on_track" | "warning" | "over_budget";
  transactionCount: number;
}
```

Module to be created — public API (locked):
```ts
export const ESSENTIAL_CATEGORIES = ['Rent', 'Utilities', 'Food', 'Transport'] as const;
export function computeSafeToSpend(
  accounts: DbAccount[],
  budgetProgress: BudgetProgress[]
): number;
```
</interfaces>
</context>

<feature>
  <name>Pure Safe-to-Spend calculation</name>
  <files>
    src/lib/safe-to-spend.ts (CREATE),
    tests/lib/safe-to-spend.test.ts (MODIFY — replace it.todo with real it() cases)
  </files>
  <behavior>
    Given `accounts: DbAccount[]` and `budgetProgress: BudgetProgress[]`, return the Safe-to-Spend EUR amount per the formula in D-S2S-01..03:

    1. EMPTY: `computeSafeToSpend([], [])` → 0
    2. ONLY-LIQUID-ACCOUNTS: accounts = [checking €1000, savings €500], budgetProgress = [] → 1500
    3. ACCOUNT-TYPE-FILTER: accounts = [checking €1000, credit €-200, investment €5000, savings €500], budgetProgress = [] → 1500 (credit + investment excluded)
    4. ESSENTIAL-SUBTRACTION: accounts = [checking €2000], budgetProgress = [Rent remaining €800, Food remaining €200] → 1000 (subtracts only essentials)
    5. DISCRETIONARY-NOT-SUBTRACTED: accounts = [checking €1000], budgetProgress = [Entertainment remaining €500, Shopping remaining €100] → 1000 (discretionary categories don't reduce S2S)
    6. ALREADY-CLAMPED-REMAINING: budgetProgress with `remainingAmount: 0` (i.e. over-budget already clamped upstream) contributes 0 — does NOT inflate S2S
    7. CLAMP-RESULT-AT-ZERO: accounts = [checking €100], budgetProgress = [Rent remaining €500] → 0 (not -400)
    8. ESSENTIAL-CATEGORIES const: `expect(ESSENTIAL_CATEGORIES).toEqual(['Rent', 'Utilities', 'Food', 'Transport'])` and verify it's readonly (`as const`) via TypeScript inference

    Boundary case: `accounts = [{ ..., balance: undefined as any }]` — implementation uses `acc.balance ?? 0` per Pattern 3 of RESEARCH.md. Test this with one explicit case.

    Negative-balance defense: an account with `balance: -100` is summed as-is (no clamping) — overdraft is legitimate data. Test with one case.
  </behavior>
  <implementation>
    File structure (mirror src/lib/budgets/index.ts header + section pattern):

    1. Multi-line JSDoc file header — title "Safe-to-Spend Calculation Module" — explains:
       - WHAT: computes the discretionary EUR amount available this month
       - FORMULA (block comment showing the math)
       - WHY: ADHD users need a calm, glanceable "how much can I spend" — not raw balance, not category-by-category
       - LIMITATIONS: hardcoded ESSENTIAL_CATEGORIES list (not user-configurable); calendar-month only; doesn't subtract credit-card debt (mock doesn't model it cleanly); doesn't account for in-flight transactions still being categorized
       - `@see docs/ADHD_UX_RESEARCH.md` for Safe-to-Spend rationale
       - `@see docs/DESIGN_SYSTEM.md §7.2` for placement context
       - `// TODO (v3+ milestone): Move ESSENTIAL_CATEGORIES to per-budget is_essential boolean on bb_budgets. Requires ADR + schema migration + RLS update + Settings UI. See 08-CONTEXT.md "Deferred Ideas".`

    2. Section divider then imports:
       - `import type { DbAccount } from "@/lib/db/types";`
       - `import type { BudgetProgress } from "@/types/finance";`

    3. Section divider "Constants" then `ESSENTIAL_CATEGORIES` const with JSDoc explaining ADHD rationale (mirror BUDGET_THRESHOLDS pattern in src/lib/budgets/index.ts:44-49). Use the const tuple `['Rent', 'Utilities', 'Food', 'Transport'] as const` — these 4 strings exactly. Add a type alias `type EssentialCategory = typeof ESSENTIAL_CATEGORIES[number];`.

    4. Section divider "Pure Calculation" then `export function computeSafeToSpend(...)`. Inline-comment each step:
       - Step 1: filter accounts where `account_type === 'checking' || account_type === 'savings'`; sum `balance ?? 0`. Comment: `// D-S2S-03: credit is debt, investment is illiquid — exclude both.`
       - Step 2: filter budgetProgress where `(ESSENTIAL_CATEGORIES as readonly string[]).includes(p.budget.category)`; sum `p.remainingAmount`. Comment: `// D-S2S-02: BudgetProgress.remainingAmount is already Math.max(0, limit - spent) per src/lib/budgets/index.ts:174 — DO NOT re-clamp here.`
       - Step 3: return `Math.max(0, totalBalance - essentialCommitted)`. Comment: `// Result-level clamp: a user with €100 balance and €500 Rent commitment shouldn't see -€400 ('safe to overdraft'). 0 communicates the truth: no headroom.`

    5. NO orchestrating `getSafeToSpend()` wrapper in this plan — page-component owns DB orchestration. Add comment near the bottom: `// NOTE: This module exports ONLY the pure compute function. Page components fetch accounts + BudgetProgress via Promise.all and call computeSafeToSpend(...) directly. No DB code lives here.`

    No `"use client"` directive. No React imports. No DOM access. This is `lib/`, not `utils/` (per CLAUDE.md project structure), because the conceptual ownership is "business logic" (the essential-categories policy), not "stateless helper".

    Test file rewrite — replace the `it.todo()` placeholders from Wave 0 with real cases. Use `as DbAccount` and `as BudgetProgress` casts to build mock objects (only include fields the function actually reads — `account_type`, `balance` for accounts; `budget.category`, `remainingAmount` for progress). Mirror the `mockBudgetProgress` fixture style from tests/components/budget-progress.test.tsx. Place all fixtures inside the describe block (not module-top) so test isolation is preserved.
  </implementation>

  <read_first>
    - src/lib/budgets/index.ts (lines 1-49) — file header style + Constants section + BUDGET_THRESHOLDS pattern to mirror
    - src/lib/budgets/index.ts (lines 158-192) — calculateAllBudgetProgress pattern (the two-layer split that this module's pure-function-only design follows); confirms remainingAmount clamp at line 174
    - src/lib/db/types.ts (lines 19-31) — DbAccount shape; confirm `account_type` is `string` not enum
    - src/types/finance.ts (lines 11-45, 196-220 if present) — ExpenseCategory + BudgetProgress shape
    - tests/utils/charts.test.ts (lines 1-37) — exact test structure (docblock + imports + section dividers + describe + it style)
    - tests/components/budget-progress.test.tsx (lines 24-70) — fixture object pattern for BudgetProgress mock data
    - tests/lib/safe-to-spend.test.ts (current state, post Wave 0) — the it.todo() stubs being replaced
    - .planning/phases/08-home-hub/08-CONTEXT.md (D-S2S-01..03) — the locked formula
    - .planning/phases/08-home-hub/08-RESEARCH.md (§ Code Examples Example 1, § Common Pitfalls 1 & 2, § Nyquist Sampling Rationale) — reference implementation + pitfalls + test list
    - .planning/phases/08-home-hub/08-PATTERNS.md (§ "src/lib/safe-to-spend.ts" entire entry) — pattern map
  </read_first>

  <acceptance_criteria>
    - `src/lib/safe-to-spend.ts` exports exactly two symbols: `ESSENTIAL_CATEGORIES` (readonly tuple of the 4 strings) and `computeSafeToSpend` (function)
    - File header docblock contains the words "FORMULA", "LIMITATIONS", and "TODO (v3+ milestone)"
    - `tests/lib/safe-to-spend.test.ts` contains at least 7 `it()` cases (no `it.todo()` remaining for `computeSafeToSpend`)
    - `bun run test tests/lib/safe-to-spend.test.ts` exits 0
    - `bun run typecheck` exits 0 (no `any` casts in production code; only in test fixtures as `as DbAccount`/`as BudgetProgress`)
    - Reading the source file, the filter expression for liquid accounts uses the literal strings `'checking'` and `'savings'` and reads `acc.account_type` (NOT `acc.type`)
    - Reading the source file, the essential-commitment subtraction uses `p.remainingAmount` directly (no `Math.max(0, p.budget.monthlyLimit - p.spentAmount)` re-calculation — the comment explaining this MUST be present)
    - The result expression contains `Math.max(0, totalBalance - essentialCommitted)` (final clamp)
    - No `"use client"` directive in the file
    - No React, no `next/`, no `@radix-ui` imports
    - The 2 TDD commits exist on the branch (RED commit references failing test count, GREEN commit references passing test count)
  </acceptance_criteria>

  <verify>
    <automated>bun run test tests/lib/safe-to-spend.test.ts --reporter=verbose 2>&1 | tail -30 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>

  <done>
    All 7+ test cases pass; the source file matches the file-header + section-divider + import-style conventions of `src/lib/budgets/index.ts`; the public API matches the locked interface; both RED and GREEN commits are on the branch; typecheck clean.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure function input | DbAccount[] and BudgetProgress[] come from authenticated Supabase queries (RLS enforces user-isolation upstream). This module makes no trust decisions of its own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | computeSafeToSpend returning a wrong value due to type confusion between `acc.account_type` (DbAccount) vs `acc.type` (Account DTO) | mitigate | Unit tests pin the field name `account_type`; reading-the-source acceptance criterion enforces it; TypeScript catches the wrong field name at compile time. |
| T-08-01-02 | Information Disclosure | None — the function is called only inside server components after RLS-filtered queries; no PII passes through this module. | accept | Low risk; module returns a number. |
| T-08-01-03 | Tampering | Negative-result clamp removed in future refactor would let S2S go negative, giving false "overdraft headroom" signal | mitigate | Explicit unit test (case 7: CLAMP-RESULT-AT-ZERO) pins the `Math.max(0, ...)` final clamp behavior. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan; vitest already vetted. |
</threat_model>

<verification>
- `bun run test tests/lib/safe-to-spend.test.ts` shows ≥ 7 passing tests
- `bun run typecheck` exits 0
- Source file has the required file-header sections and inline comments referencing D-S2S-01..03 and src/lib/budgets/index.ts:174
- No `"use client"`, no React, no `@radix-ui`
</verification>

<success_criteria>
- TDD discipline observed: RED commit exists before GREEN commit
- All 7 Nyquist test cases pass
- Public API matches the locked interface
- Future readers can understand the formula from the file header alone
</success_criteria>

<output>
Create `.planning/phases/08-home-hub/08-01-SUMMARY.md` when done. Include the 2 commit SHAs (RED and GREEN), the final test count, and any deviation from the planned test cases.
</output>
