---
phase: 06-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03

must_haves:
  truths:
    - "All --bb-* foundation color tokens (TOKEN-01) are available in both light and dark mode"
    - "All --bb-* semantic financial color tokens (TOKEN-02) are available in both light and dark mode, with --bb-negative as soft coral not bright red"
    - "All 7 budget category color tokens (TOKEN-03) are defined"
    - "No existing shadcn/ui blocks are modified — changes are strictly additive"
    - "bun run build exits 0"
  artifacts:
    - path: "src/app/globals.css"
      provides: "BB color token :root and .dark blocks"
      contains:
        - "/* BetterBudgeter Design System Tokens"
        - "--bb-bg: oklch("
        - "--bb-negative: oklch(0.58 0.16 25)"
        - "--bb-cat-essentials: oklch("
        - "/* BetterBudgeter Design System Tokens — Dark Mode"
  key_links:
    - from: "src/app/globals.css BB :root block"
      to: "src/app/globals.css BB .dark block"
      via: "CSS cascade — .dark class on <html> overrides :root values"
      pattern: "--bb-bg: oklch"
---

<objective>
Add the BetterBudgeter color token definitions to globals.css — foundation colors (TOKEN-01), semantic financial colors (TOKEN-02), and budget category colors (TOKEN-03) — in both light mode (:root) and dark mode (.dark) blocks.

Purpose: These are the raw CSS custom property values that all subsequent phases (7–11) consume to build components and pages with a consistent, ADHD-optimized color palette.

Output: Two new CSS blocks in src/app/globals.css (BB :root block and BB .dark block), placed after the existing shadcn :root and .dark blocks respectively. All 21 color tokens defined (6 foundation + 8 semantic with -bg variants + 7 category).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-tokens/06-CONTEXT.md
@.planning/phases/06-design-tokens/06-UI-SPEC.md
@.planning/phases/06-design-tokens/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BB color tokens to :root block (light mode)</name>

  <read_first>
    - src/app/globals.css — read the full file first; the new block inserts after line 82 (closing brace of the shadcn :root block). Do NOT modify lines 48–82.
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Color — exact oklch values for all foundation, semantic, and category tokens in light mode
    - .planning/phases/06-design-tokens/06-PATTERNS.md Pattern 2 — exact block template with comments to replicate
  </read_first>

  <action>
    Insert a new :root block immediately after line 82 (the closing `}` of the existing shadcn :root block) in src/app/globals.css. Do NOT touch lines 1–82. The new block starts on line 84 (pushing the existing .dark block and everything below down).

    The new block must contain exactly these token groups in this order, with these comment headers:

    Header comment: `/* BetterBudgeter Design System Tokens — Light Mode`
    (follow with the multi-line comment from PATTERNS.md Pattern 2 explaining the two-step pattern)

    GROUP 1 — Foundation colors comment: `/* Foundation colors (TOKEN-01) — 60/30/10 split`
    Tokens (per D-03, UI-SPEC.md §Color/Foundation):
    - --bb-bg
    - --bb-surface
    - --bb-surface-raised
    - --bb-text
    - --bb-text-secondary
    - --bb-border

    GROUP 2 — Semantic financial colors comment: `/* Semantic financial colors (TOKEN-02)`
    Include inline note: `--bb-negative is soft coral, NEVER bright red — avoids ADHD anxiety/shame response`
    Tokens:
    - --bb-positive
    - --bb-positive-bg
    - --bb-caution
    - --bb-caution-bg
    - --bb-negative
    - --bb-negative-bg
    - --bb-info
    - --bb-info-bg

    GROUP 3 — Category colors comment: `/* Budget category colors (TOKEN-03) — for charts and indicators only`
    Include note: `Less saturated than Tailwind defaults. Same values in .dark (chart context).`
    Tokens:
    - --bb-cat-essentials
    - --bb-cat-discretionary
    - --bb-cat-savings
    - --bb-cat-food
    - --bb-cat-transport
    - --bb-cat-entertainment
    - --bb-cat-other

    All exact oklch values are in UI-SPEC.md §Color. Copy them precisely — do not invent or approximate values.

    The existing shadcn .dark block (currently at line 84) must appear immediately after the new :root block closes. Do not reorder or remove any existing content.
  </action>

  <verify>
    <automated>
      grep -c "^  --bb-" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: at least 21 (6 foundation + 8 semantic + 7 category).

    <automated>
      grep "BetterBudgeter Design System Tokens — Light Mode" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (non-empty output).

    <automated>
      grep "bb-negative: oklch(0.58 0.16 25)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (confirms soft coral, not bright red).

    <automated>
      grep "bb-cat-entertainment" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (all 7 category tokens present).

    <automated>
      grep "background: oklch(1 0 0)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: still present (confirms shadcn :root is untouched).
  </verify>

  <acceptance_criteria>
    - src/app/globals.css contains a new :root block with comment header "BetterBudgeter Design System Tokens — Light Mode"
    - The block defines --bb-bg, --bb-surface, --bb-surface-raised, --bb-text, --bb-text-secondary, --bb-border (6 foundation tokens)
    - The block defines --bb-positive, --bb-positive-bg, --bb-caution, --bb-caution-bg, --bb-negative, --bb-negative-bg, --bb-info, --bb-info-bg (8 semantic tokens)
    - --bb-negative value is exactly `oklch(0.58 0.16 25)` (soft coral)
    - The block defines --bb-cat-essentials, --bb-cat-discretionary, --bb-cat-savings, --bb-cat-food, --bb-cat-transport, --bb-cat-entertainment, --bb-cat-other (7 category tokens)
    - The existing shadcn :root block (lines 48–82) is byte-for-byte unchanged — `--background: oklch(1 0 0)` still present at its original location
    - `grep -c "^  --bb-" src/app/globals.css` returns >= 21
  </acceptance_criteria>

  <done>New BB :root color block inserted after line 82, containing all 21 color tokens for TOKEN-01, TOKEN-02, TOKEN-03 in light mode values. Existing shadcn :root block untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add BB color tokens to .dark block (dark mode overrides)</name>

  <read_first>
    - src/app/globals.css — read after Task 1 completes; the BB .dark block inserts after the existing shadcn .dark block (which now ends at a different line number — find the closing `}` of `.dark { --sidebar-ring: ... }`)
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Color — dark mode oklch values for all foundation and semantic tokens (TOKEN-01/02); category tokens use same values as light
    - .planning/phases/06-design-tokens/06-PATTERNS.md Pattern 3 — exact block template for .dark with comments
  </read_first>

  <action>
    Insert a new .dark block immediately after the closing `}` of the existing shadcn .dark block. Do NOT modify any line inside the existing shadcn .dark block (--background through --sidebar-ring dark values).

    The new block must contain exactly these token groups in this order:

    Header comment: `/* BetterBudgeter Design System Tokens — Dark Mode`
    (follow with the multi-line comment from PATTERNS.md Pattern 3: dark mode activated by next-themes via .dark class on html, @custom-variant registered at line 5, category colors same in dark)

    GROUP 1 — comment: `/* Foundation colors — dark mode`
    Dark mode values (from UI-SPEC.md §Color/Foundation dark column):
    - --bb-bg
    - --bb-surface
    - --bb-surface-raised
    - --bb-text
    - --bb-text-secondary
    - --bb-border

    GROUP 2 — comment: `/* Semantic financial colors — dark mode`
    Dark mode values (from UI-SPEC.md §Color/Semantic dark column):
    - --bb-positive
    - --bb-positive-bg
    - --bb-caution
    - --bb-caution-bg
    - --bb-negative (dark value: `oklch(0.68 0.18 25)`)
    - --bb-negative-bg
    - --bb-info
    - --bb-info-bg

    GROUP 3 — comment: `/* Category colors — same as light mode (chart context provides own contrast)`
    Same oklch values as :root (unchanged in dark):
    - --bb-cat-essentials through --bb-cat-other (all 7)

    All exact oklch values are in UI-SPEC.md §Color. Do not approximate.
  </action>

  <verify>
    <automated>
      grep "BetterBudgeter Design System Tokens — Dark Mode" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "bb-negative: oklch(0.68 0.18 25)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (dark mode soft coral value).

    <automated>
      grep "bb-bg: oklch(0.155 0.005 280)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (dark mode background).

    <automated>
      grep "sidebar-ring: oklch(0.442 0.017 285.786)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: still present (confirms existing shadcn .dark block untouched).

    <automated>
      cd /Users/paulheuwer/dev/01-Projects/BetterBudgeter && bun run build 2>&1 | tail -5
    </automated>
    Expected: build exits 0 / "Compiled successfully" or similar — no CSS parse errors.
  </verify>

  <acceptance_criteria>
    - src/app/globals.css contains a new .dark block with comment header "BetterBudgeter Design System Tokens — Dark Mode"
    - Dark mode block defines all 6 foundation tokens with dark-mode oklch values (--bb-bg dark value: `oklch(0.155 0.005 280)`)
    - Dark mode block defines all 8 semantic tokens with dark-mode values (--bb-negative dark: `oklch(0.68 0.18 25)`)
    - Dark mode block repeats all 7 category tokens with the same values as :root (chart context manages contrast)
    - The existing shadcn .dark block is untouched — `--background: oklch(0.141 0.005 285.823)` still present at its original location
    - `bun run build` exits 0 with no CSS errors
    - Two .dark blocks now exist in the file: the shadcn one (unchanged) and the new BB one
  </acceptance_criteria>

  <done>BB .dark color override block inserted after the shadcn .dark block, containing all 21 color tokens in dark mode values for TOKEN-01, TOKEN-02, TOKEN-03. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file edit | Developer edits globals.css; no user-controlled input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | globals.css shadcn blocks | mitigate | Plan instructs executor to NOT modify existing shadcn blocks; acceptance criteria grep-verify originals are present byte-for-byte |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — CSS-only, no new dependencies |

Note: This is a pure CSS phase. There is no user input, no authentication surface, no data storage, no API calls, and no JavaScript execution. The only meaningful risk is incorrect oklch values producing inaccessible contrast ratios — mitigated by the UI-SPEC.md providing pre-verified exact values derived from the design system spec.
</threat_model>

<verification>
After both tasks complete, verify the full color token set:

```bash
# Count BB tokens in :root (should be >= 21 color tokens)
grep -c "^  --bb-" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Verify light mode block header present
grep "BetterBudgeter Design System Tokens — Light Mode" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Verify dark mode block header present
grep "BetterBudgeter Design System Tokens — Dark Mode" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Verify soft coral (not bright red) for --bb-negative
grep "bb-negative: oklch(0.58 0.16 25)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Verify shadcn :root untouched
grep "background: oklch(1 0 0)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Build check
cd /Users/paulheuwer/dev/01-Projects/BetterBudgeter && bun run build
```
</verification>

<success_criteria>
- src/app/globals.css contains two new blocks: a BB :root block and a BB .dark block
- All 21 color tokens defined in :root (light): 6 foundation (TOKEN-01) + 8 semantic with -bg variants (TOKEN-02) + 7 category (TOKEN-03)
- All 21 color tokens overridden in .dark (dark): foundation and semantic use dark-specific values; category tokens reuse light values
- --bb-negative light = `oklch(0.58 0.16 25)`, dark = `oklch(0.68 0.18 25)` — soft coral confirmed
- Original shadcn :root and .dark blocks are byte-for-byte unchanged
- `bun run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/06-design-tokens/06-01-SUMMARY.md` when done.
</output>
