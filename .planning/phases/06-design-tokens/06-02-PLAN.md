---
phase: 06-design-tokens
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - TOKEN-04
  - TOKEN-05
  - TOKEN-06
  - TOKEN-07

must_haves:
  truths:
    - "Spacing scale tokens --bb-space-1 through --bb-space-12 (9 steps) are defined in :root"
    - "Border radius tokens --bb-radius-sm through --bb-radius-xl are defined in :root"
    - "Shadow tokens --bb-shadow-sm/md/lg are defined in :root"
    - "Font stack tokens --bb-font-sans and --bb-font-mono are defined in :root"
    - "Type scale tokens --bb-text-sm, --bb-text-base, --bb-text-xl, --bb-text-3xl are defined in :root"
    - "Body base style applies font-size: 16px and line-height: 1.5 globally (TOKEN-04)"
    - "bun run build exits 0"
  artifacts:
    - path: "src/app/globals.css"
      provides: "BB spacing, radius, shadow, typography tokens + base body style"
      contains:
        - "--bb-space-1: 4px"
        - "--bb-radius-sm: 6px"
        - "--bb-shadow-sm: 0 1px 2px"
        - "--bb-font-sans: \"Lucida Sans Unicode\""
        - "--bb-text-base: 16px"
        - "font-size: 16px"
        - "line-height: 1.5"
  key_links:
    - from: "src/app/globals.css BB :root spacing/type tokens"
      to: "@layer base body{}"
      via: "TOKEN-04 — --bb-text-base value (16px) is the specification source for the body rule"
      pattern: "font-size: 16px"
---

<objective>
Extend the BB :root token block (created in Plan 01) with spacing, border radius, shadow, font stack, and type scale tokens — then apply TOKEN-04 body style globally.

Purpose: Completes the raw CSS custom property layer for all non-color token categories (TOKEN-04 through TOKEN-07). Plan 03 will then expose all of these as Tailwind utilities.

Output: The BB :root block gains 19 additional token declarations (9 spacing + 4 radius + 3 shadows + 2 font stacks + 4 type scale sizes). The @layer base body rule gains `font-size: 16px; line-height: 1.5;`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-tokens/06-CONTEXT.md
@.planning/phases/06-design-tokens/06-UI-SPEC.md
@.planning/phases/06-design-tokens/06-PATTERNS.md
@.planning/phases/06-design-tokens/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BB :root block with spacing, radius, shadow, and typography tokens</name>

  <read_first>
    - src/app/globals.css — read the full file as it stands after Plan 01. Locate the BB :root block (the one with comment "BetterBudgeter Design System Tokens — Light Mode"). The new token groups are appended inside this existing BB :root block, after the --bb-cat-other line and before the closing `}`.
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Spacing — 9 spacing token values (4px grid)
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Color/Border Radius Tokens — 4 radius values
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Color/Shadow Tokens — 3 shadow values
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Typography — font stacks (D-05/D-06) and type scale
    - .planning/phases/06-design-tokens/06-PATTERNS.md Pattern 2 — the full :root template showing these groups inside the BB :root block
  </read_first>

  <action>
    Append the following token groups inside the existing BB :root block (the one labeled "BetterBudgeter Design System Tokens — Light Mode") — add them after the `--bb-cat-other` line, before the block's closing `}`.

    Do NOT create a new :root block. Do NOT touch the shadcn :root block or the BB .dark block.

    GROUP 4 — Spacing scale comment: `/* Spacing scale (TOKEN-05) — 4px grid`
    Include note: `Steps 7, 9, 11 are intentionally omitted (not in spec).`
    Tokens and values (exact — from UI-SPEC.md §Spacing):
    - --bb-space-1: 4px
    - --bb-space-2: 8px
    - --bb-space-3: 12px
    - --bb-space-4: 16px
    - --bb-space-5: 20px
    - --bb-space-6: 24px
    - --bb-space-8: 32px
    - --bb-space-10: 40px
    - --bb-space-12: 48px

    GROUP 5 — Border radius comment: `/* Border radius (TOKEN-06)`
    Tokens and values (from UI-SPEC.md §Color/Border Radius):
    - --bb-radius-sm: 6px
    - --bb-radius-md: 10px
    - --bb-radius-lg: 14px
    - --bb-radius-xl: 20px

    GROUP 6 — Shadow tokens comment: `/* Shadow tokens (TOKEN-06) — intentionally minimal`
    Include note: `Prefer border + background for elevation over heavy shadows`
    Tokens and values (from UI-SPEC.md §Color/Shadow Tokens):
    - --bb-shadow-sm: 0 1px 2px oklch(0 0 0 / 0.04)
    - --bb-shadow-md: 0 2px 8px oklch(0 0 0 / 0.06)
    - --bb-shadow-lg: 0 4px 16px oklch(0 0 0 / 0.08)

    GROUP 7 — Font stacks comment: `/* Font stacks (TOKEN-07)`
    Include notes (per PATTERNS.md Pattern 2):
    - "--bb-font-sans: Lucida-led prototype — open letterforms aid ADHD legibility."
    - "Verdana fallback: designed for screen legibility, best b/d differentiation."
    - "System tail: covers all remaining platforms."
    - "NOTE: This is a prototype. Finalize in Phase 11."
    Tokens and values (from UI-SPEC.md §Typography / CONTEXT.md D-05/D-06):
    - --bb-font-sans: "Lucida Sans Unicode", "Lucida Grande", "Lucida Sans", Verdana, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      (the long value wraps to a second line — see PATTERNS.md Pattern 2 for the exact formatting)
    - --bb-font-mono: ui-monospace, "SF Mono", "Cascadia Code", Menlo, monospace

    GROUP 8 — Type scale comment: `/* Type scale (TOKEN-07) — 4 sizes, 2 weights only (minimalism for ADHD clarity)`
    Tokens and values (from UI-SPEC.md §Typography/Type Scale):
    - --bb-text-sm: 14px
    - --bb-text-base: 16px
    - --bb-text-xl: 22px
    - --bb-text-3xl: 36px
  </action>

  <verify>
    <automated>
      grep "bb-space-1: 4px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "bb-space-12: 48px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (confirms all 9 spacing steps present, not just first).

    <automated>
      grep "bb-radius-xl: 20px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "bb-shadow-lg: 0 4px 16px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep 'bb-font-sans: "Lucida Sans Unicode"' /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (Lucida-led prototype stack).

    <automated>
      grep "bb-text-3xl: 36px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep -c "^  --bb-" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: >= 40 (21 color tokens from Plan 01 + 19 new tokens this plan, inside :root; dark mode repeats don't match ^  --bb- pattern since .dark uses same indentation).
  </verify>

  <acceptance_criteria>
    - `grep "bb-space-1: 4px"` returns a match (spacing scale starts)
    - `grep "bb-space-12: 48px"` returns a match (spacing scale ends)
    - `grep "bb-space-7"` returns NO match (step 7 intentionally omitted)
    - `grep "bb-radius-sm: 6px"` returns a match
    - `grep "bb-radius-xl: 20px"` returns a match
    - `grep "bb-shadow-sm: 0 1px 2px"` returns a match
    - `grep 'bb-font-sans: "Lucida Sans Unicode"'` returns a match (D-05 prototype stack)
    - `grep "bb-text-base: 16px"` returns a match
    - `grep "bb-text-3xl: 36px"` returns a match
    - Only ONE BB :root block exists (append, not new block) — `grep -c "BetterBudgeter Design System Tokens — Light Mode"` returns 1
    - Shadcn :root block unchanged — `grep "background: oklch(1 0 0)"` still present
  </acceptance_criteria>

  <done>BB :root block extended with spacing (9 tokens), radius (4), shadow (3), font stacks (2), type scale (4) token groups — 19 additional tokens covering TOKEN-05, TOKEN-06, TOKEN-07.</done>
</task>

<task type="auto">
  <name>Task 2: Apply TOKEN-04 base body style in @layer base</name>

  <read_first>
    - src/app/globals.css — find the @layer base block (currently containing `* { @apply border-border outline-ring/50; }` and `body { @apply bg-background text-foreground; }`). The body rule must be extended in-place — NOT replaced.
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Typography/Base Body Style — exact CSS to add (font-size: 16px; line-height: 1.5;)
    - .planning/phases/06-design-tokens/06-PATTERNS.md Pattern 4 — the exact extended body block with comment text
    - .planning/phases/06-design-tokens/06-CONTEXT.md D-07 — rationale: 16px is browser default (no regression), 1.5 is modest global improvement
  </read_first>

  <action>
    Extend the existing `body {}` rule inside `@layer base` by adding two CSS declarations AFTER the existing `@apply bg-background text-foreground;` line. Do NOT remove the @apply line. Do NOT create a separate body rule.

    The extended body rule must look like this (from PATTERNS.md Pattern 4):
    ```
    body {
      @apply bg-background text-foreground;
      /* TOKEN-04: Minimum 16px body text + 1.5x line-height for ADHD readability.
         16px is browser default — no visual regression expected.
         1.5 line-height is a modest global improvement to reading comfort. */
      font-size: 16px;
      line-height: 1.5;
    }
    ```

    Add those two declarations and the comment block inside the existing body rule. The `* { @apply border-border outline-ring/50; }` rule must remain unchanged above it.
  </action>

  <verify>
    <automated>
      grep "font-size: 16px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found inside @layer base body.

    <automated>
      grep "line-height: 1.5" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "bg-background text-foreground" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (original @apply line preserved, not replaced).

    <automated>
      grep "TOKEN-04" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (comment referencing TOKEN-04 is present per junior-dev-friendly requirement).

    <automated>
      cd /Users/paulheuwer/dev/01-Projects/BetterBudgeter && bun run build 2>&1 | tail -5
    </automated>
    Expected: exits 0 with no CSS errors.
  </verify>

  <acceptance_criteria>
    - `@layer base body {}` rule now contains `font-size: 16px;` and `line-height: 1.5;` after the existing `@apply bg-background text-foreground;` line
    - The original `@apply bg-background text-foreground;` line is preserved (not removed or replaced)
    - The `* { @apply border-border outline-ring/50; }` rule is untouched
    - A comment referencing TOKEN-04 and explaining the rationale is present on the lines before font-size (junior-dev readable)
    - `bun run build` exits 0
    - Only one body rule exists in @layer base — `grep -c "bg-background text-foreground"` returns 1
  </acceptance_criteria>

  <done>@layer base body rule extended with font-size: 16px and line-height: 1.5. TOKEN-04 satisfied. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file edit | Developer edits globals.css; no user-controlled input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | globals.css shadcn :root and @layer base | mitigate | Plan instructs in-place append/extend; acceptance criteria verify original lines still present |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — CSS-only, no new dependencies |

Note: Pure CSS phase. No security-relevant attack surface. Incorrect token values (e.g. wrong px values in spacing) would cause layout inconsistency in later phases — not a security issue. Mitigated by the acceptance criteria requiring exact value matches.
</threat_model>

<verification>
After both tasks complete:

```bash
# Spacing scale — start and end
grep "bb-space-1: 4px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "bb-space-12: 48px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Step 7 intentionally absent
grep "bb-space-7" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
# Expected: no output

# Radius and shadows
grep "bb-radius-xl: 20px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "bb-shadow-lg: 0 4px 16px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Font and type scale
grep 'bb-font-sans: "Lucida Sans Unicode"' /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "bb-text-3xl: 36px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Body style
grep "font-size: 16px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "line-height: 1.5" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Build
cd /Users/paulheuwer/dev/01-Projects/BetterBudgeter && bun run build
```
</verification>

<success_criteria>
- BB :root block contains 40 total token declarations (21 color from Plan 01 + 9 spacing + 4 radius + 3 shadow + 2 font stacks + 4 type scale)
- --bb-space-7, --bb-space-9, --bb-space-11 are NOT defined (intentional per spec)
- @layer base body rule has font-size: 16px and line-height: 1.5 alongside the existing @apply
- Comment referencing TOKEN-04 explains the rationale inline
- `bun run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/06-design-tokens/06-02-SUMMARY.md` when done.
</output>
