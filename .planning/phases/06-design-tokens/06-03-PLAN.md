---
phase: 06-design-tokens
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04
  - TOKEN-05
  - TOKEN-06
  - TOKEN-07

must_haves:
  truths:
    - "All --bb-* tokens are accessible as Tailwind utility classes (bg-bb-*, text-bb-*, p-bb-*, rounded-bb-*, text-bb-*)"
    - "The BB @theme inline block is separate from and placed after the existing shadcn @theme inline block"
    - "The BB @theme inline block is labeled with comment 'BetterBudgeter Tailwind Mappings'"
    - "Color mappings follow shadcn naming convention: --color-bb-{name}: var(--bb-{name})"
    - "Spacing mappings use --spacing-bb-{n}: var(--bb-space-{n}) convention"
    - "Type scale mappings cover all 5 sizes (xs through 3xl) plus lh and fw companions"
    - "bun run build exits 0 with no CSS errors"
  artifacts:
    - path: "src/app/globals.css"
      provides: "BB @theme inline block with all Tailwind utility mappings"
      contains:
        - "/* BetterBudgeter Tailwind Mappings"
        - "--color-bb-bg: var(--bb-bg)"
        - "--color-bb-negative: var(--bb-negative)"
        - "--spacing-bb-4: var(--bb-space-4)"
        - "--radius-bb-lg: var(--bb-radius-lg)"
        - "--text-bb-xs: var(--bb-text-xs)"
        - "--text-bb-3xl: var(--bb-text-3xl)"
        - "--text-bb-base-lh: var(--bb-text-base-lh)"
        - "--text-bb-xl-fw: var(--bb-text-xl-fw)"
  key_links:
    - from: "src/app/globals.css BB @theme inline block"
      to: "src/app/globals.css BB :root block"
      via: "var() references — @theme mappings point to :root custom properties"
      pattern: "--color-bb-bg: var(--bb-bg)"
    - from: "src/app/globals.css BB @theme inline block"
      to: "Tailwind v4 utility classes"
      via: "@theme inline registration — Tailwind reads this block and generates utility classes"
      pattern: "--color-bb-"
---

<objective>
Add the BetterBudgeter @theme inline block to globals.css — mapping all --bb-* CSS custom properties to Tailwind v4 utility classes so subsequent phases can use classes like `bg-bb-surface`, `text-bb-text`, `p-bb-4`, `rounded-bb-lg`, and `text-bb-3xl` in components.

Purpose: This is Step 2 of the two-step token registration pattern (Step 1 was the :root/:dark blocks in Plans 01–02). Without this block, all --bb-* tokens exist as CSS variables but are not available as Tailwind utility classes. After this plan, the full token system is operative.

Output: One new @theme inline block inserted immediately after the existing shadcn @theme inline block. The block maps all 21 color tokens (TOKEN-01/02/03), 9 spacing tokens (TOKEN-05), 4 radius tokens (TOKEN-06), and the full typography set (TOKEN-07: 5 size tokens + 5 lh companions + 5 fw companions = 15 typography mappings) to Tailwind utilities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-tokens/06-CONTEXT.md
@.planning/phases/06-design-tokens/06-UI-SPEC.md
@.planning/phases/06-design-tokens/06-PATTERNS.md
@.planning/phases/06-design-tokens/06-01-SUMMARY.md
@.planning/phases/06-design-tokens/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert BB @theme inline block with all Tailwind utility mappings</name>

  <read_first>
    - src/app/globals.css — read the full current file. The new BB @theme inline block inserts immediately after the closing `}` of the existing shadcn @theme inline block (the one that starts with `--color-background: var(--background)` and ends with `--radius-xl: calc(var(--radius) + 4px)`). Do NOT modify lines inside the existing shadcn @theme block.
    - .planning/phases/06-design-tokens/06-CONTEXT.md D-01/D-02 — naming convention: --color-bb-{name}, --spacing-bb-{n}, --radius-bb-{name}, --text-bb-{name}
    - .planning/phases/06-design-tokens/06-PATTERNS.md Pattern 1 — the complete @theme inline block template with all mapping groups and comments
    - .planning/phases/06-design-tokens/06-UI-SPEC.md §Color/Tailwind Color Mappings and §Spacing/Tailwind Mapping columns — confirms the mapping names
  </read_first>

  <action>
    Insert a new @theme inline block immediately after the closing `}` of the existing shadcn @theme inline block. Do NOT touch any line inside the existing shadcn @theme block.

    The new block opens with a multi-line comment (per PATTERNS.md Pattern 1):
    ```
    /* BetterBudgeter Tailwind Mappings
       Two-step pattern: token defined in :root → exposed here as Tailwind utility.
       Naming convention (D-02): --color-bb-{name} → bg-bb-{name}, text-bb-{name}
                                  --spacing-bb-{n}  → p-bb-{n}, m-bb-{n}
                                  --radius-bb-{name}→ rounded-bb-{name}
                                  --text-bb-{name}  → text-bb-{name}
    */
    @theme inline {
    ```

    Inside the block, define these mapping groups in order:

    GROUP A — comment: `/* Foundation colors (TOKEN-01) */`
    Mappings (D-02 naming: --color-bb-{name}: var(--bb-{name})):
    - --color-bb-bg: var(--bb-bg)
    - --color-bb-surface: var(--bb-surface)
    - --color-bb-surface-raised: var(--bb-surface-raised)
    - --color-bb-text: var(--bb-text)
    - --color-bb-text-secondary: var(--bb-text-secondary)
    - --color-bb-border: var(--bb-border)

    GROUP B — comment: `/* Semantic financial colors (TOKEN-02) */`
    Mappings:
    - --color-bb-positive: var(--bb-positive)
    - --color-bb-positive-bg: var(--bb-positive-bg)
    - --color-bb-caution: var(--bb-caution)
    - --color-bb-caution-bg: var(--bb-caution-bg)
    - --color-bb-negative: var(--bb-negative)
    - --color-bb-negative-bg: var(--bb-negative-bg)
    - --color-bb-info: var(--bb-info)
    - --color-bb-info-bg: var(--bb-info-bg)

    GROUP C — comment: `/* Budget category colors (TOKEN-03) */`
    Mappings:
    - --color-bb-cat-essentials: var(--bb-cat-essentials)
    - --color-bb-cat-discretionary: var(--bb-cat-discretionary)
    - --color-bb-cat-savings: var(--bb-cat-savings)
    - --color-bb-cat-food: var(--bb-cat-food)
    - --color-bb-cat-transport: var(--bb-cat-transport)
    - --color-bb-cat-entertainment: var(--bb-cat-entertainment)
    - --color-bb-cat-other: var(--bb-cat-other)

    GROUP D — comment: `/* Spacing scale (TOKEN-05) — 4px grid, named steps */`
    Mappings (D-02 naming: --spacing-bb-{n}: var(--bb-space-{n})):
    - --spacing-bb-1: var(--bb-space-1)
    - --spacing-bb-2: var(--bb-space-2)
    - --spacing-bb-3: var(--bb-space-3)
    - --spacing-bb-4: var(--bb-space-4)
    - --spacing-bb-5: var(--bb-space-5)
    - --spacing-bb-6: var(--bb-space-6)
    - --spacing-bb-8: var(--bb-space-8)
    - --spacing-bb-10: var(--bb-space-10)
    - --spacing-bb-12: var(--bb-space-12)
    Note: steps 7, 9, 11 are omitted — matching the :root omissions.

    GROUP E — comment: `/* Border radius (TOKEN-06) */`
    Mappings (D-02 naming: --radius-bb-{name}: var(--bb-radius-{name})):
    - --radius-bb-sm: var(--bb-radius-sm)
    - --radius-bb-md: var(--bb-radius-md)
    - --radius-bb-lg: var(--bb-radius-lg)
    - --radius-bb-xl: var(--bb-radius-xl)

    GROUP F — comment: `/* Typography scale (TOKEN-07) — 5 sizes, with line-height and font-weight companions */`

    Sub-comment: `/* Size tokens — font-size values */`
    Size mappings (D-02 naming: --text-bb-{name}: var(--bb-text-{name})):
    - --text-bb-xs: var(--bb-text-xs)
    - --text-bb-sm: var(--bb-text-sm)
    - --text-bb-base: var(--bb-text-base)
    - --text-bb-xl: var(--bb-text-xl)
    - --text-bb-3xl: var(--bb-text-3xl)

    Sub-comment: `/* Line-height companions — one per size */`
    Line-height mappings:
    - --text-bb-xs-lh: var(--bb-text-xs-lh)
    - --text-bb-sm-lh: var(--bb-text-sm-lh)
    - --text-bb-base-lh: var(--bb-text-base-lh)
    - --text-bb-xl-lh: var(--bb-text-xl-lh)
    - --text-bb-3xl-lh: var(--bb-text-3xl-lh)

    Sub-comment: `/* Font-weight companions — one per size */`
    Font-weight mappings:
    - --text-bb-xs-fw: var(--bb-text-xs-fw)
    - --text-bb-sm-fw: var(--bb-text-sm-fw)
    - --text-bb-base-fw: var(--bb-text-base-fw)
    - --text-bb-xl-fw: var(--bb-text-xl-fw)
    - --text-bb-3xl-fw: var(--bb-text-3xl-fw)

    Close the block with `}`.

    Important: Shadow tokens (--bb-shadow-*) do NOT get @theme inline mappings — Tailwind v4 does not have a --shadow-* convention for arbitrary shadow values. The shadows are consumed directly via CSS custom properties (var(--bb-shadow-sm) etc.) in component styles.

    The existing shadcn @theme inline block must remain unchanged above the new block. Verify by checking that `--color-background: var(--background)` is still the first entry in the shadcn block.
  </action>

  <verify>
    <automated>
      grep "BetterBudgeter Tailwind Mappings" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (BB @theme block header comment present).

    <automated>
      grep -c "^  --color-bb-" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: >= 21 (6 foundation + 8 semantic + 7 category).

    <automated>
      grep "color-bb-bg: var(--bb-bg)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "color-bb-negative: var(--bb-negative)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "spacing-bb-4: var(--bb-space-4)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "radius-bb-lg: var(--bb-radius-lg)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "text-bb-xs: var(--bb-text-xs)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (xs size mapping).

    <automated>
      grep "text-bb-3xl: var(--bb-text-3xl)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found.

    <automated>
      grep "text-bb-xs-lh: var(--bb-text-xs-lh)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (xs line-height companion mapping).

    <automated>
      grep "text-bb-base-lh: var(--bb-text-base-lh)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (base line-height companion mapping).

    <automated>
      grep "text-bb-xl-fw: var(--bb-text-xl-fw)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (xl font-weight companion mapping — heavy weight).

    <automated>
      grep "text-bb-3xl-fw: var(--bb-text-3xl-fw)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (3xl font-weight companion mapping).

    <automated>
      grep "color-background: var(--background)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: line found (shadcn @theme block unchanged).

    <automated>
      grep -c "@theme inline" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
    </automated>
    Expected: 2 (shadcn block + BB block — each @theme inline declaration is its own line).

    <automated>
      cd /Users/paulheuwer/dev/01-Projects/BetterBudgeter && bun run build 2>&1 | tail -10
    </automated>
    Expected: exits 0, no "Invalid CSS" or parse errors.
  </verify>

  <acceptance_criteria>
    - A new @theme inline block with comment header "BetterBudgeter Tailwind Mappings" is present
    - The block is positioned AFTER the existing shadcn @theme inline block (not before it, not replacing it)
    - `grep -c "@theme inline"` returns 2 (two separate @theme blocks)
    - `grep -c "^  --color-bb-"` returns >= 21 (all color mappings present)
    - `grep "spacing-bb-12: var(--bb-space-12)"` returns a match (all 9 spacing steps mapped)
    - `grep "spacing-bb-7"` returns NO match (step 7 omitted, matching :root)
    - `grep "radius-bb-sm: var(--bb-radius-sm)"` and `grep "radius-bb-xl: var(--bb-radius-xl)"` both return matches (all 4 radius tokens)
    - `grep "text-bb-xs: var(--bb-text-xs)"` returns a match (xs size — bottom of scale)
    - `grep "text-bb-sm: var(--bb-text-sm)"` and `grep "text-bb-3xl: var(--bb-text-3xl)"` both return matches (all 5 type scale sizes)
    - `grep "text-bb-xs-lh: var(--bb-text-xs-lh)"` returns a match (xs lh companion)
    - `grep "text-bb-3xl-lh: var(--bb-text-3xl-lh)"` returns a match (3xl lh companion)
    - `grep "text-bb-xs-fw: var(--bb-text-xs-fw)"` returns a match (xs fw companion)
    - `grep "text-bb-xl-fw: var(--bb-text-xl-fw)"` returns a match (xl fw companion — heavy weight)
    - `grep "shadow-bb"` returns NO match (shadow tokens intentionally not in @theme inline)
    - `grep "color-background: var(--background)"` still present (shadcn @theme block untouched)
    - `bun run build` exits 0 with no CSS parse errors
  </acceptance_criteria>

  <done>BB @theme inline block inserted after the shadcn @theme block, mapping all BB token references to Tailwind utility classes: 21 color tokens + 9 spacing + 4 radius + 15 typography mappings (5 sizes + 5 lh + 5 fw) = 49 total mappings. Full two-step token system is operative. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file edit | Developer edits globals.css; no user-controlled input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | globals.css shadcn @theme inline block | mitigate | Plan instructs insert-after, not modify; acceptance criteria verify `--color-background: var(--background)` still present and @theme count is exactly 2 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — CSS-only, no new dependencies |

Note: Pure CSS phase with no user input or execution surface. Incorrect @theme mapping names (e.g. `--colour-bb-bg` typo) would silently fail to generate Tailwind classes, caught by visual verification in browser DevTools in subsequent phases. The acceptance criteria grep checks provide early detection.
</threat_model>

<verification>
Final Phase 6 verification — run after Plan 03 completes:

```bash
# Two @theme inline blocks exist
grep -c "@theme inline" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
# Expected: 2

# All color mappings present (21+)
grep -c "^  --color-bb-" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
# Expected: >= 21

# Spacing, radius mappings
grep "spacing-bb-4: var(--bb-space-4)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "radius-bb-lg: var(--bb-radius-lg)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Typography — all 5 sizes
grep "text-bb-xs: var(--bb-text-xs)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "text-bb-3xl: var(--bb-text-3xl)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Line-height companions
grep "text-bb-xs-lh: var(--bb-text-xs-lh)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "text-bb-base-lh: var(--bb-text-base-lh)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "text-bb-3xl-lh: var(--bb-text-3xl-lh)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Font-weight companions
grep "text-bb-xl-fw: var(--bb-text-xl-fw)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "text-bb-3xl-fw: var(--bb-text-3xl-fw)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Soft coral for --bb-negative (not bright red)
grep "bb-negative: oklch(0.58 0.16 25)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# TOKEN-04 body style
grep "font-size: 16px" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "line-height: 1.5" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Shadcn blocks untouched
grep "color-background: var(--background)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css
grep "background: oklch(1 0 0)" /Users/paulheuwer/dev/01-Projects/BetterBudgeter/src/app/globals.css

# Full build
cd /Users/paulheuwer/dev/01-Projects/BetterBudgeter && bun run build
```
</verification>

<success_criteria>
Phase 6 is complete when:
1. globals.css contains 2 @theme inline blocks (shadcn unchanged + new BB block)
2. globals.css contains 2 :root blocks (shadcn unchanged + BB block with 54 tokens)
3. globals.css contains 2 .dark blocks (shadcn unchanged + BB block with 21 dark values)
4. @layer base body rule has font-size: 16px and line-height: 1.5 (TOKEN-04)
5. All TOKEN-01 through TOKEN-07 requirements covered:
   - TOKEN-01: --bb-bg, --bb-surface, --bb-surface-raised, --bb-text, --bb-text-secondary, --bb-border
   - TOKEN-02: --bb-positive/caution/negative/info with -bg variants, soft coral confirmed
   - TOKEN-03: all 7 --bb-cat-* tokens
   - TOKEN-04: body font-size: 16px + line-height: 1.5 in @layer base
   - TOKEN-05: --bb-space-1 through --bb-space-12 (9 steps, no 7/9/11)
   - TOKEN-06: --bb-radius-sm/md/lg/xl + --bb-shadow-sm/md/lg
   - TOKEN-07: --bb-font-sans (Lucida prototype) + --bb-font-mono + type scale 5 sizes (xs/sm/base/xl/3xl) + 5 lh companions + 5 fw companions
6. All Tailwind utility mappings registered in BB @theme inline block (49 total: 21 color + 9 spacing + 4 radius + 15 typography)
7. `bun run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/06-design-tokens/06-03-SUMMARY.md` when done.
</output>
