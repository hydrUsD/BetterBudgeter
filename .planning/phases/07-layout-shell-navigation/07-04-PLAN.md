---
phase: 07-layout-shell-navigation
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - src/app/(bb)/layout.tsx
  - src/app/(bb)/page.tsx
  - src/app/(bb)/settings/page.tsx
  - src/app/(bb)/budgets/page.tsx
  - src/app/(bb)/transactions/page.tsx
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-05
  - NAV-06
must_haves:
  truths:
    - "D-06: src/app/(bb)/layout.tsx exists as a server component composing <PageShell>{children}<TabBar /></PageShell> exactly per CONTEXT.md D-06 spec (BB chrome — no legacy providers)"
    - "D-01: The (bb)/ route group exists and houses the 4 BB pages, completing D-01's two-route-group split alongside (legacy)/ from Plan 03"
    - "D-07: Home (src/app/page.tsx) is moved via git mv to src/app/(bb)/page.tsx with its outer <main> element downgraded to <div> (Pitfall 5 mitigation) — per D-07 move list"
    - "D-07: Settings (src/app/settings/page.tsx) is moved via git mv to src/app/(bb)/settings/page.tsx with its outer <main> downgraded to <div> — per D-07 'existing settings stays untouched (content)'"
    - "D-07 + D-08: Stub page src/app/(bb)/budgets/page.tsx exists rendering <PageHeader title='Budgets' subtitle='Track your monthly spending limits' /> + neutral placeholder 'Your budgets will appear here.' (D-08 locked copy verbatim)"
    - "D-07 + D-08: Stub page src/app/(bb)/transactions/page.tsx exists rendering <PageHeader title='Transactions' subtitle='Your spending history' /> + neutral placeholder 'Your transactions will appear here.' (D-08 locked copy verbatim)"
    - "D-06: All 4 BB URLs (/, /budgets, /transactions, /settings) render the TabBar at the bottom of the viewport via (bb)/layout.tsx composition"
    - "D-06: Active tab on each BB URL highlights the correct tab in --bb-info color with filled icon (TabBar from Plan 02 mounted inside D-06's (bb)/layout.tsx)"
    - "D-02: /login and /link-bank render WITHOUT the TabBar (standalone — NAV-06 fully verified per D-02; standalone pages stay outside both groups)"
    - "All 5 legacy URLs still render WITH legacy chrome (D-04) and WITHOUT the TabBar (NAV-04 still satisfied — verifies D-01 route group isolation)"
    - "D-13: src/middleware.ts remains byte-identical — route groups do not affect matchers; this plan's moves preserve URL identity for /, /settings, /login, /link-bank"
    - "D-14: /dashboard 308 redirect to / still works after Home moved into (bb)/page.tsx — the redirect target / continues to resolve (D-14 outcome verified end-to-end in Task 3 smoke test)"
    - "Stub pages include a SECURITY NOTE comment reminding future phases to add /budgets and /transactions to middleware matchers when real data lands"
  artifacts:
    - path: "src/app/(bb)/layout.tsx"
      provides: "(D-06) BB route group layout — composes <PageShell>{children}<TabBar /></PageShell> per CONTEXT.md D-06"
      contains: "PageShell"
    - path: "src/app/(bb)/page.tsx"
      provides: "(D-07) Relocated Home page via git mv (content unchanged except <main> → <div>)"
    - path: "src/app/(bb)/settings/page.tsx"
      provides: "(D-07) Relocated Settings page via git mv (content unchanged except <main> → <div>; D-07 'existing settings stays untouched')"
    - path: "src/app/(bb)/budgets/page.tsx"
      provides: "(D-07 + D-08) New stub Budgets page — PageHeader + locked D-08 placeholder body"
      contains: "PageHeader"
    - path: "src/app/(bb)/transactions/page.tsx"
      provides: "(D-07 + D-08) New stub Transactions page — PageHeader + locked D-08 placeholder body"
      contains: "PageHeader"
  key_links:
    - from: "src/app/(bb)/layout.tsx"
      to: "src/components/layout/PageShell.tsx"
      via: "import { PageShell } from '@/components/layout/PageShell' (D-06 composition outer)"
      pattern: "PageShell.*@/components/layout/PageShell"
    - from: "src/app/(bb)/layout.tsx"
      to: "src/components/layout/TabBar.tsx"
      via: "import { TabBar } from '@/components/layout/TabBar' (D-06 sibling to {children})"
      pattern: "TabBar.*@/components/layout/TabBar"
    - from: "src/app/(bb)/budgets/page.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "import { PageHeader } from '@/components/layout/PageHeader' (D-08 stub uses PageHeader)"
      pattern: "PageHeader.*@/components/layout/PageHeader"
    - from: "src/app/(bb)/transactions/page.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "import { PageHeader } from '@/components/layout/PageHeader' (D-08 stub uses PageHeader)"
      pattern: "PageHeader.*@/components/layout/PageHeader"
---

<objective>
Wire up the BB chrome: create `src/app/(bb)/layout.tsx` per **D-06** that consumes `PageShell` + `TabBar` (built in Plans 01/02), relocate the existing Home and Settings pages into `(bb)/` per **D-07** (with the `<main>` → `<div>` downgrade needed to avoid double-`<main>` per RESEARCH §Pitfall 5), and create the two stub pages for `/budgets` and `/transactions` per **D-07 + D-08**. This is the final wiring step that makes all 6 NAV requirements observable end-to-end in the browser.

Purpose: Plans 01/02 built the layout primitives (D-11, D-12, D-09, D-10). Plan 03 cleared the legacy + root structural ground (D-01 partial, D-02, D-03, D-04, D-05, D-13, D-14). Plan 04 completes D-01's two-route-group split by adding the `(bb)/` half, then lights up the BB experience: a user navigating to `/`, `/budgets`, `/transactions`, or `/settings` now sees the 4-tab bottom navigation per D-06, the correct active-tab highlighting per D-10, and the PageShell-constrained content per D-11. Standalone pages (`/login`, `/link-bank`) and legacy pages remain TabBar-free as required (D-02 + D-04).

Output:
- `src/app/(bb)/layout.tsx` — server component composing `<PageShell>{children}<TabBar /></PageShell>` per **D-06**
- `src/app/(bb)/page.tsx` — Home page (relocated from `src/app/page.tsx` via `git mv` per **D-07**, with outer `<main>` downgraded to `<div>` to avoid double-`<main>`)
- `src/app/(bb)/settings/page.tsx` — Settings page (relocated from `src/app/settings/page.tsx` via `git mv` per **D-07**, same `<main>` → `<div>` edit; content otherwise unchanged per D-07 'existing settings stays untouched')
- `src/app/(bb)/budgets/page.tsx` — new stub per **D-07 + D-08** (locked copy: title 'Budgets', subtitle 'Track your monthly spending limits', body 'Your budgets will appear here.')
- `src/app/(bb)/transactions/page.tsx` — new stub per **D-07 + D-08** (locked copy: title 'Transactions', subtitle 'Your spending history', body 'Your transactions will appear here.')
- Human-verify checkpoint smoke-testing all 11 routes (4 BB + 5 legacy + 2 standalone) verifying D-02, D-06, D-13, D-14 end-to-end

This plan finalizes requirements **NAV-01, NAV-02, NAV-03** (TabBar now visible and active on BB routes via D-06), **NAV-05** (PageShell + PageHeader now consumed by real pages), and **NAV-06** (standalone pages confirmed TabBar-free per D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 7 source artifacts
@.planning/phases/07-layout-shell-navigation/07-CONTEXT.md
@.planning/phases/07-layout-shell-navigation/07-RESEARCH.md
@.planning/phases/07-layout-shell-navigation/07-PATTERNS.md
@.planning/phases/07-layout-shell-navigation/07-UI-SPEC.md
@.planning/phases/07-layout-shell-navigation/07-VALIDATION.md

# Dependencies from Plan 01 + Plan 02
@src/components/layout/PageShell.tsx
@src/components/layout/PageHeader.tsx
@src/components/layout/TabBar.tsx

# Files being moved or edited
@src/app/page.tsx
@src/app/settings/page.tsx

# Pattern analogs for stub pages and metadata
@src/app/legacy-index/page.tsx
@src/lib/head.ts

# Prior plan summaries (only those genuinely needed)
@.planning/phases/07-layout-shell-navigation/07-03-SUMMARY.md

<interfaces>
<!-- Contracts consumed by this plan (from Plans 01, 02, 03). Read these once; do not re-derive. -->

// From Plan 01 (D-11 + D-12):
export interface PageShellProps {
  children: React.ReactNode;
  className?: string;
}
export function PageShell(props: PageShellProps): JSX.Element;

export interface PageHeaderProps {
  title: string;
  subtitle?: string;
  className?: string;
}
export function PageHeader(props: PageHeaderProps): JSX.Element;

// From Plan 02 (D-09 + D-10):
export function TabBar(): JSX.Element;  // "use client" — uses usePathname()

// From Plan 03 (structural state after Plan 03 — D-01 partial, D-02, D-03, D-04, D-05, D-13, D-14):
// - src/app/layout.tsx is slim per D-05 (html/body/ThemeProvider/Toaster, viewportFit:cover)
// - src/app/(legacy)/layout.tsx exists per D-04 with full legacy chrome
// - 5 legacy pages live under src/app/(legacy)/... per D-03
// - src/app/page.tsx and src/app/settings/page.tsx still exist at their OLD paths (Plan 04 moves them per D-07)
// - src/app/link-bank/page.tsx and src/app/(auth)/login/page.tsx are unmoved (correct per D-02 — standalone)
// - src/middleware.ts byte-identical to pre-Phase-7 (D-13)

// From src/lib/head.ts (existing helper):
export function generateMetadata(opts?: { title?: string }): Metadata;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (bb)/layout.tsx per D-06 + move Home & Settings into (bb)/ per D-07 with <main> → <div> downgrade</name>
  <files>src/app/(bb)/layout.tsx, src/app/(bb)/page.tsx, src/app/(bb)/settings/page.tsx</files>
  <read_first>
    - src/app/page.tsx (full current Home page — confirm `<main className="flex flex-col gap-6 p-6 max-w-5xl mx-auto">` is at line 99 and `</main>` is at line 304)
    - src/app/settings/page.tsx (full current Settings page — confirm `<main className="flex flex-col gap-6 p-6 max-w-2xl mx-auto">` is at line 61 and `</main>` is at line 165)
    - src/components/layout/PageShell.tsx (created by Plan 01 — confirm export name and that it renders `<main>`)
    - src/components/layout/TabBar.tsx (created by Plan 02 — confirm export name and `"use client"` directive)
    - .planning/phases/07-layout-shell-navigation/07-CONTEXT.md D-06, D-07 (lines 60-73 — locks layout body and the 4 file moves)
    - .planning/phases/07-layout-shell-navigation/07-RESEARCH.md §Code Example 2 (lines 573-597 — verbatim BB layout reference)
    - .planning/phases/07-layout-shell-navigation/07-RESEARCH.md §Pitfall 5 (lines 490-496 — double-<main> avoidance: must edit moved pages)
    - .planning/phases/07-layout-shell-navigation/07-PATTERNS.md §src/app/(bb)/layout.tsx (lines 134-173 — verbatim implementation)
    - .planning/phases/07-layout-shell-navigation/07-PATTERNS.md §src/app/(bb)/page.tsx (lines 486-509 — exact <main> → <div> edit instructions)
    - .planning/phases/07-layout-shell-navigation/07-PATTERNS.md §src/app/(bb)/settings/page.tsx (lines 512-533 — exact <main> → <div> edit)
  </read_first>
  <action>
    Step 1 — Create `src/app/(bb)/layout.tsx` per **D-06** (completes D-01 two-route-group split):

    Ensure the parent directory exists: `mkdir -p "src/app/(bb)"` (quote because `(` is shell metacharacter). This creates the (bb) half of D-01's two-route-group split.

    Write the file per PATTERNS lines 134-173 and RESEARCH §Code Example 2:
    - Do NOT include `"use client"` (per RESEARCH §Pitfall 4 — the layout stays a server component; only TabBar carries the client boundary per D-10).
    - BB-style JSDoc header comment per the style of `BudgetProgressSection.tsx` lines 1-14, explaining: "BB App Layout (D-06) — applied to all pages in the (bb) route group: /, /budgets, /transactions, /settings. Composes <PageShell>{children}<TabBar /></PageShell> exactly per CONTEXT.md D-06. NOTE per D-06: 'No legacy providers (AppProvider / BudgetProvider) — BB pages don't use them.' IMPORTANT: This is a SERVER component. Do not add 'use client'. The interactive TabBar is itself a client component — that's where the boundary lives. Keeping the layout server-side preserves RSC benefits for the BB pages (data fetching, etc.)."
    - Imports (D-06 composition):
      ```
      import { PageShell } from "@/components/layout/PageShell";
      import { TabBar } from "@/components/layout/TabBar";
      ```
    - Default export `function BBLayout({ children }: { children: React.ReactNode })` returning EXACTLY the D-06 composition:
      ```
      <PageShell>
        {children}
        <TabBar />
      </PageShell>
      ```

    Step 2 — `git mv` Home page into `(bb)/` per **D-07** entry 1:

    `git mv src/app/page.tsx "src/app/(bb)/page.tsx"`

    Step 3 — Edit the moved Home page to downgrade `<main>` to `<div>` (Pitfall 5 mitigation — PageShell from D-06 layout now provides the page's `<main>`):
    - Open `src/app/(bb)/page.tsx`.
    - Find the outer `<main className="flex flex-col gap-6 p-6 max-w-5xl mx-auto">` (was line 99 pre-move). Replace it with `<div className="flex flex-col gap-6">` — keep the inner flex/gap classes (page-content rhythm) but drop `p-6 max-w-5xl mx-auto` because PageShell (D-11) now owns padding and max-width.
    - Find the matching closing `</main>` (was line 304 pre-move) and replace with `</div>`.
    - Verify with `grep -c '<main\|</main>' "src/app/(bb)/page.tsx"` — must return `0` (no `<main>` tags remain in the page; PageShell will provide the single `<main>` for the route).
    - Add a one-line inline comment immediately above the `<div>` explaining: `// Outer wrapper downgraded from <main> to <div> per Phase 7 D-07 — PageShell (in (bb)/layout.tsx per D-06) now provides the single <main> landmark for this route. See RESEARCH §Pitfall 5.`

    Step 4 — `git mv` Settings page into `(bb)/` per **D-07** entry 2:

    The parent dir `src/app/(bb)/settings/` does not exist; `git mv` of a directory creates it.

    `git mv src/app/settings "src/app/(bb)/settings"`

    Step 5 — Edit the moved Settings page to downgrade `<main>` to `<div>`:
    - Open `src/app/(bb)/settings/page.tsx`.
    - Find the outer `<main className="flex flex-col gap-6 p-6 max-w-2xl mx-auto">` (was line 61 pre-move). Replace with `<div className="flex flex-col gap-6">`.
    - Find the matching closing `</main>` (was line 165 pre-move) and replace with `</div>`.
    - Verify `grep -c '<main\|</main>' "src/app/(bb)/settings/page.tsx"` returns `0`.
    - Add the same one-line inline comment as Home above the new `<div>`.
    - **Do NOT** restructure the body of the settings page to use `PageHeader` — that is explicitly out of Phase 7 scope per **D-07** ("existing settings stays untouched — Phase 9 expands"). The page's body stays exactly as it was; only the outer `<main>` element is downgraded.

    Step 6 — Verify the build resolves all routes including the BB chrome (D-06 + D-13: URLs preserved despite file moves):
    - `bun run typecheck` — must pass.
    - `bun run build` — must pass. Watch specifically for:
      - "You cannot have two parallel pages that resolve to the same path" — would mean `(bb)/page.tsx` and a leftover `src/app/page.tsx` both resolve to `/` (Pitfall 2). Should not happen because `git mv` removed the original.
      - Tailwind warnings on any `bb-*` class — would mean Phase 6 mapping is incomplete; do NOT modify globals.css (STOP & ASK per .claude/rules/01-safety-and-workflow.md §7).
    - The build log should now include `/`, `/budgets`, `/transactions`, `/settings` as BB routes AND `/legacy`, `/analytics`, `/achievements`, `/dashboard`, `/legacy-index` as legacy routes (D-01 + D-03 + D-13: URLs unchanged) AND `/login`, `/link-bank` as standalone routes (D-02).

    Commit as `refactor(07-04): wire BB layout (D-06), move Home and Settings into (bb)/ (D-07)`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run build 2>&1 | grep -E "^[^A-Z]*[├└]\s*[/○●ƒ]\s+/(budgets|transactions|settings|login|link-bank|legacy|analytics|achievements|dashboard|legacy-index|$)"</automated>
  </verify>
  <acceptance_criteria>
    - D-06: `src/app/(bb)/layout.tsx` exists and does NOT start with `"use client"`: `head -1 "src/app/(bb)/layout.tsx" | grep -c "use client"` returns `0`
    - D-06: `src/app/(bb)/layout.tsx` imports PageShell and TabBar: `grep -c 'from "@/components/layout/PageShell"' "src/app/(bb)/layout.tsx"` returns `1` AND `grep -c 'from "@/components/layout/TabBar"' "src/app/(bb)/layout.tsx"` returns `1`
    - D-06: `src/app/(bb)/layout.tsx` renders `<PageShell>` wrapping `{children}` and `<TabBar />`: `grep -c '<PageShell>' "src/app/(bb)/layout.tsx"` returns at least `1` AND `grep -c '<TabBar' "src/app/(bb)/layout.tsx"` returns at least `1`
    - D-07: `src/app/(bb)/page.tsx` exists at the new path: `test -f "src/app/(bb)/page.tsx"` exits 0
    - D-07: Old Home path is gone: `test -f src/app/page.tsx` exits non-zero
    - Pitfall 5: Moved Home has zero `<main>` tags: `grep -c '<main\|</main>' "src/app/(bb)/page.tsx"` returns `0`
    - D-07: `src/app/(bb)/settings/page.tsx` exists: `test -f "src/app/(bb)/settings/page.tsx"` exits 0
    - D-07: Old Settings path is gone: `test -d src/app/settings` exits non-zero
    - Pitfall 5: Moved Settings has zero `<main>` tags: `grep -c '<main\|</main>' "src/app/(bb)/settings/page.tsx"` returns `0`
    - D-07: Git records both moves as renames: `git log --diff-filter=R --name-status -1 -- "src/app/(bb)/page.tsx"` shows an `R` entry; same for `src/app/(bb)/settings/page.tsx`
    - `bun run typecheck` exits 0
    - `bun run build` exits 0 with no path-collision or Tailwind warnings
    - Build output lists `/`, `/budgets` (will be added in Task 2 — only if Task 2 runs after; otherwise just `/`), `/settings` as BB routes
  </acceptance_criteria>
  <done>BB layout (D-06) wired; Home and Settings moved per D-07 and downgraded; build green. No <main> nesting violations.</done>
</task>

<task type="auto">
  <name>Task 2: Create stub pages for /budgets and /transactions per D-07 + D-08 (locked copy)</name>
  <files>src/app/(bb)/budgets/page.tsx, src/app/(bb)/transactions/page.tsx</files>
  <read_first>
    - src/components/layout/PageHeader.tsx (created by Plan 01 — D-12 contract: PageHeader signature, does NOT wrap in <main>)
    - src/app/(bb)/layout.tsx (created in Task 1 per D-06 — confirms PageShell provides the <main> for these stubs)
    - src/lib/head.ts (confirms generateMetadata({ title }) signature)
    - src/app/legacy-index/page.tsx lines 1-21 (analog: header comment + metadata export pattern)
    - .planning/phases/07-layout-shell-navigation/07-CONTEXT.md D-07 (lines 68-73 — locks the two stub creations: 'Create new: src/app/(bb)/budgets/page.tsx' and 'Create new: src/app/(bb)/transactions/page.tsx')
    - .planning/phases/07-layout-shell-navigation/07-CONTEXT.md D-08 (lines 76-79 — locks the exact stub copy: title/subtitle/body for both pages)
    - .planning/phases/07-layout-shell-navigation/07-UI-SPEC.md §Stub Pages (lines 237-247 — locks visual treatment: no icons, no dashed borders, secondary text color, fragment wrapper)
    - .planning/phases/07-layout-shell-navigation/07-UI-SPEC.md §Copywriting Contract (lines 122-145 — locks exact title and subtitle strings)
    - .planning/phases/07-layout-shell-navigation/07-RESEARCH.md §Code Example 7 (lines 816-846 — verbatim stub page reference)
    - .planning/phases/07-layout-shell-navigation/07-RESEARCH.md §Security Domain action items (lines 1005-1007 — middleware-matcher TODO note)
    - .planning/phases/07-layout-shell-navigation/07-PATTERNS.md §src/app/(bb)/budgets/page.tsx (lines 537-583)
    - .planning/phases/07-layout-shell-navigation/07-PATTERNS.md §src/app/(bb)/transactions/page.tsx (lines 587-613)
  </read_first>
  <action>
    Create both stub pages per **D-07** (create new files) and **D-08** (locked user-facing copy). Both stubs use identical structure but different copy. Both are server components, both return a React fragment (no outer `<main>` — PageShell from `(bb)/layout.tsx` per D-06 provides it), both consume `PageHeader` (D-12).

    Step 1 — Create `src/app/(bb)/budgets/page.tsx` per **D-07 + D-08**:

    Ensure directory exists: `mkdir -p "src/app/(bb)/budgets"`.

    File content:
    - BB-style JSDoc header comment explaining: "Budgets Page — Stub for Phase 7 (D-07 + D-08). This page exists in Phase 7 only so the /budgets tab has somewhere to navigate to. Content is filled in by Phase 9 (PAGE-04). Stub copy is user-facing and neutral per CONTEXT.md D-08 — exact strings locked there, do not improvise. EXTENSION POINT: Phase 9 will replace the placeholder paragraph with monthly overview, budget progress cards, spending donut chart (migrated from Home), and 'Edit budgets →' link. SECURITY NOTE: When Phase 9 adds real user data here, the /budgets URL MUST be added to src/middleware.ts matchers (currently public — only static placeholder text)."
    - Imports:
      ```
      import { PageHeader } from "@/components/layout/PageHeader";
      import { generateMetadata } from "@/lib/head";
      ```
    - `export const metadata = generateMetadata({ title: "Budgets" });` (matches the pattern from `src/app/legacy-index/page.tsx` line 18-20)
    - Default export `function BudgetsPage()` returning a React fragment with D-08 locked copy:
      ```
      <>
        <PageHeader title="Budgets" subtitle="Track your monthly spending limits" />
        <p className="text-bb-base text-bb-text-secondary">
          Your budgets will appear here.
        </p>
      </>
      ```
    - Title, subtitle, and body string are EXACTLY as locked in **D-08** and UI-SPEC §Copywriting. Do NOT improvise wording — final copy pass is Phase 11.
    - NO icons, NO dashed border, NO empty-state container (per UI-SPEC §Anti-Patterns #12 + D-08 — those visual treatments belong to the formal Empty State component in Phase 10).

    Step 2 — Create `src/app/(bb)/transactions/page.tsx` per **D-07 + D-08**:

    `mkdir -p "src/app/(bb)/transactions"`.

    Identical structure to Budgets stub, only the title/subtitle/body copy and the JSDoc references differ:
    - JSDoc header replaces "Budgets" → "Transactions", "/budgets" → "/transactions", "PAGE-04" → "PAGE-05", "monthly overview, budget progress cards, spending donut chart..." → "Income + Expenses KPI cards, full transaction history grouped by date, Sync Transactions button". Keep the same SECURITY NOTE wording with "/transactions" substituted. Reference D-07 + D-08 in the header.
    - `export const metadata = generateMetadata({ title: "Transactions" });`
    - `function TransactionsPage()` returning D-08 locked copy:
      ```
      <>
        <PageHeader title="Transactions" subtitle="Your spending history" />
        <p className="text-bb-base text-bb-text-secondary">
          Your transactions will appear here.
        </p>
      </>
      ```

    Step 3 — Verify build:
    - `bun run typecheck` — must pass.
    - `bun run build` — must pass. Build log MUST now show `/budgets` and `/transactions` as buildable routes.
    - Quick sanity check on stub copy strings (D-08 verbatim): `grep -c "Your budgets will appear here." "src/app/(bb)/budgets/page.tsx"` returns `1` AND `grep -c "Your transactions will appear here." "src/app/(bb)/transactions/page.tsx"` returns `1`.

    Commit as `feat(07-04): add stub pages for /budgets and /transactions (D-07 + D-08)`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run build 2>&1 | grep -E "/(budgets|transactions)\b"</automated>
  </verify>
  <acceptance_criteria>
    - D-07: `src/app/(bb)/budgets/page.tsx` exists: `test -f "src/app/(bb)/budgets/page.tsx"` exits 0
    - D-07: `src/app/(bb)/transactions/page.tsx` exists: `test -f "src/app/(bb)/transactions/page.tsx"` exits 0
    - Both files import PageHeader and generateMetadata: `grep -c 'from "@/components/layout/PageHeader"' "src/app/(bb)/budgets/page.tsx"` returns `1` AND same for transactions; `grep -c 'from "@/lib/head"' "src/app/(bb)/budgets/page.tsx"` returns `1` AND same for transactions
    - Both files export metadata via the helper: `grep -c 'generateMetadata({ title: "Budgets" })' "src/app/(bb)/budgets/page.tsx"` returns `1`; `grep -c 'generateMetadata({ title: "Transactions" })' "src/app/(bb)/transactions/page.tsx"` returns `1`
    - D-08: Both files include the exact stub copy strings (D-08 locked verbatim): `grep -c '"Track your monthly spending limits"' "src/app/(bb)/budgets/page.tsx"` returns `1`; `grep -c '"Your spending history"' "src/app/(bb)/transactions/page.tsx"` returns `1`; `grep -c "Your budgets will appear here." "src/app/(bb)/budgets/page.tsx"` returns `1`; `grep -c "Your transactions will appear here." "src/app/(bb)/transactions/page.tsx"` returns `1`
    - Body `<p>` uses BB tokens: `grep -c "text-bb-base text-bb-text-secondary" "src/app/(bb)/budgets/page.tsx"` returns at least `1`; same for transactions
    - Neither file has a `<main>` tag (PageShell from D-06 layout provides it): `grep -c '<main' "src/app/(bb)/budgets/page.tsx"` returns `0`; same for transactions
    - Neither file has `"use client"` (server components): `grep -c '"use client"' "src/app/(bb)/budgets/page.tsx"` returns `0`; same for transactions
    - SECURITY NOTE comment is present (per RESEARCH §Security Domain action items): `grep -c "SECURITY NOTE" "src/app/(bb)/budgets/page.tsx"` returns at least `1`; same for transactions
    - `bun run typecheck` exits 0
    - `bun run build` exits 0 and includes `/budgets` and `/transactions` in the route table
  </acceptance_criteria>
  <done>Both stub pages exist per D-07 + D-08, render the locked title/subtitle/body, include security TODO comments, and are buildable routes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end smoke test — all 11 URLs verify NAV-01..06 + D-02, D-06, D-13, D-14 in the browser</name>
  <what-built>
    The full Phase 7 layout shell is wired:
    - `(bb)/layout.tsx` per **D-06** wraps Home, Budgets, Transactions, Settings with PageShell + TabBar
    - Home page (`/`) and Settings page (`/settings`) have been moved into `(bb)/` per **D-07** and their outer `<main>` downgraded to `<div>` (PageShell now provides the single `<main>` landmark per route)
    - Stub pages exist for `/budgets` and `/transactions` per **D-07 + D-08** rendering PageHeader + locked placeholder copy
    - Legacy routes (`/legacy`, `/analytics`, `/achievements`, `/dashboard`, `/legacy-index`) continue to render with their full legacy chrome (D-04) and WITHOUT the TabBar
    - Standalone routes (`/login`, `/link-bank`) render under the slim root only per **D-02** — no TabBar, no legacy chrome
    - `viewportFit: "cover"` is set in the root Viewport export (D-05) so iOS safe-area-inset-bottom resolves correctly under the TabBar
    - `src/middleware.ts` is byte-identical per **D-13** — URLs preserved through route group restructure

    This checkpoint verifies every NAV-* requirement is observable in the browser before declaring Phase 7 complete, AND verifies D-14 end-to-end (`/dashboard` 308 still redirects to `/` even after Home moved into `(bb)/`).
  </what-built>
  <how-to-verify>
    Start the dev server: `bun dev` (default port 3031).

    Run through this 11-URL checklist. Open each URL in a fresh tab and visually confirm the listed behaviors. A clean browser session (incognito) is recommended to avoid stale state.

    **BB routes (4) — must show the TabBar at the bottom of the viewport per D-06:**

    1. http://localhost:3031/  (Home)
       - [ ] Bottom tab bar visible with 4 tabs: Home, Budgets, Transactions, Settings (NAV-01, D-10)
       - [ ] "Home" tab is active: label in soft blue (`--bb-info`) AND House icon is FILLED (not outlined) (NAV-02, D-10)
       - [ ] Other 3 tabs are in muted secondary color with OUTLINED icons (NAV-02, D-10)
       - [ ] Tab bar is constrained to max 768px and centered on wide screens (D-09) — resize the window to confirm it stays centered, not stretched to viewport edges
       - [ ] Page content (greeting, dashboard sections) renders correctly above the tab bar
       - [ ] Content does NOT sit underneath the tab bar (PageShell bottom padding works — D-11)
       - [ ] No double `<main>` tag in DOM (DevTools → Elements → search for "main" — should find exactly 1)

    2. http://localhost:3031/budgets
       - [ ] Tab bar visible per D-06; "Budgets" tab active (label `--bb-info`, ChartPie icon filled) (NAV-02, D-10)
       - [ ] Page shows `<h1>Budgets</h1>` and the subtitle "Track your monthly spending limits" (D-08 locked copy)
       - [ ] Below the header, the placeholder paragraph "Your budgets will appear here." is visible in secondary text color (D-08 locked copy)
       - [ ] No icons, no dashed boxes, no decorative chrome (D-08 + UI-SPEC §Stub Pages)
       - [ ] Tapping "Home" instantly returns to `/` with no transition animation (NAV-03, D-10)

    3. http://localhost:3031/transactions
       - [ ] Tab bar visible per D-06; "Transactions" tab active (label `--bb-info`, List icon filled per D-10)
       - [ ] Page shows `<h1>Transactions</h1>` and subtitle "Your spending history" (D-08 locked copy)
       - [ ] Body shows "Your transactions will appear here." in secondary text (D-08 locked copy)
       - [ ] Tab switches are instant — no fade, slide, or any animation between routes (NAV-03, D-10)

    4. http://localhost:3031/settings
       - [ ] Tab bar visible per D-06; "Settings" tab active (label `--bb-info`, Settings/Gear icon filled per D-10)
       - [ ] The existing settings page content renders (budget settings, etc.) — content was NOT restructured in Phase 7 per **D-07** ("existing settings stays untouched")

    **Visual / interaction checks (apply to any BB route):**
    - [ ] All 4 tab tap targets feel comfortable (≥44×44px per A11Y-01 + D-10)
    - [ ] Keyboard focus ring visible when tabbing through tabs (Tab key cycles through them) — if invisible, note it; the UI-SPEC Open Q4 anticipated this might need a fallback `focus-visible:ring-2`
    - [ ] In dark mode (toggle via legacy ThemeToggle on `/legacy`, then return to `/`): tab bar colors invert correctly — both `--bb-info` and `--bb-text-secondary` remain readable on `--bb-surface` (A11Y-04)
    - [ ] Lucide filled-icon visual quality: confirm each filled icon looks like a recognizable filled shape, not a solid blob (RESEARCH Open Q2). If House, ChartPie, List, or Settings filled state looks bad, note which one — the fallback per RESEARCH §Open Questions §2 is to swap to an alternative lucide icon (e.g. `Home`, `Menu`, `Cog`); that fix would be a follow-up.

    **Legacy routes (5) — must show LEGACY chrome (D-04: Logo + Settings + Achievements + ThemeToggle) and must NOT show the TabBar (NAV-04 verifies D-01 route-group isolation):**

    5. http://localhost:3031/legacy — legacy chrome present (D-04), NO TabBar (NAV-04 / D-01)
    6. http://localhost:3031/analytics — legacy chrome present (D-04), NO TabBar (NAV-04 / D-01)
    7. http://localhost:3031/achievements — legacy chrome present (D-04), NO TabBar (NAV-04 / D-01)
    8. http://localhost:3031/legacy-index — legacy chrome present (D-04), NO TabBar (NAV-04 / D-01)
    9. http://localhost:3031/dashboard — 308 redirects to `/` per **D-14** (this is the end-to-end D-14 verification: redirect survived the D-03 move AND the new `/` route lives at `(bb)/page.tsx` after D-07 move). Verify also via terminal: `curl -I http://localhost:3031/dashboard` returns `HTTP/1.1 308` with `Location: /`.

    **Standalone routes (2) — must show NEITHER TabBar NOR legacy chrome per D-02 (NAV-06):**

    10. http://localhost:3031/login — login form only, no TabBar, no Logo bar, no PasscodeWrapper (D-02)
    11. http://localhost:3031/link-bank — if you are signed in, the bank-linking flow renders without TabBar or legacy chrome (D-02); if not signed in, middleware redirects you to `/login` (which is also correct — middleware behavior preserved per **D-13**)

    **iOS safe-area check (if you have access to iOS Safari or Chrome DevTools iPhone preset with notch — optional but valuable):**
    - [ ] On a notched iPhone preset, the TabBar should sit ABOVE the home indicator (not flush against it). If the tab bar overlaps the home indicator, `viewportFit: "cover"` may not have been added correctly to the root Viewport export (D-05) — re-verify Plan 03 Task 1.

    If any check fails, do not approve. Describe exactly which URL and which symptom in the resume signal.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 11 URLs pass their respective checks (verifying D-02, D-06, D-13, D-14 + NAV-01..06). Otherwise describe the failure (URL + symptom). For each failed URL, include any console errors and the active tab label/icon state observed. If lucide filled-icon visual quality is poor on one of the 4 icons (per RESEARCH Open Q2), note which icon — that fix is a documented Phase 11 follow-up, not a Phase 7 blocker.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → middleware → BB route group | `/` and `/settings/:path*` are in the middleware matcher list (auth-gated). After this plan, those URLs resolve to `(bb)/page.tsx` and `(bb)/settings/page.tsx` — file paths changed per D-07, URLs unchanged, so middleware behavior is preserved (D-13 + RESEARCH §Pitfall 7). `/budgets` and `/transactions` are NOT in the matcher list — acceptable in Phase 7 because they contain only static placeholder text per D-08 (no user data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Elevation of Privilege | Stub pages public access | accept | `/budgets` and `/transactions` stubs render only static placeholder copy per D-08 — no PII, no auth-gated data, no DB queries. Acceptable per RESEARCH §Security Domain V4 row. Both stub page files include a SECURITY NOTE JSDoc comment instructing future phases to add these URLs to the middleware matcher when real data arrives (Phase 9 PAGE-04/05 work). |
| T-07-11 | Tampering | Stub-page placeholder copy | accept | All strings are hard-coded JSX text in server components (D-08 locked); React escapes by default. No user input is rendered. |
| T-07-12 | Information Disclosure | TabBar visible on stub pages | accept | TabBar reveals only the route names (Home, Budgets, Transactions, Settings) per D-10 which are public surfaces. Same exposure as having a navigation menu — no sensitive data exposed. |
| T-07-13 | Elevation of Privilege | Home/Settings relocation auth bypass | mitigate | D-07 + D-13: `/` and `/settings/:path*` matchers in middleware match URLs, not file paths. After `git mv` per D-07 the URLs are unchanged, so auth gating continues to work. Task 3 smoke test step 1 + step 4 implicitly verifies auth still works (visiting `/` while unauthenticated must redirect per existing middleware behavior). |
| T-07-14 | Information Disclosure (double-`<main>`) | Moved Home / Settings pages | mitigate | RESEARCH §Pitfall 5 — Task 1 explicitly downgrades each page's outer `<main>` to `<div>` and verifies with `grep -c '<main\|</main>'` returning 0. PageShell from `(bb)/layout.tsx` (D-06) provides the single `<main>` landmark per route. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (PageShell, PageHeader, TabBar built in Plans 01/02 use only existing deps; layout/page files import only in-tree modules and existing helpers). RESEARCH §Package Legitimacy Audit confirms zero new dependencies. Slopcheck not required. |

**Security posture:** Phase 7 remains security-neutral. The structural move preserves all existing auth/middleware behavior per D-13. Future-phase TODO embedded in stub-page JSDoc comments captures the requirement to add `/budgets` and `/transactions` to middleware when Phase 9 adds real data.
</threat_model>

<verification>
After all 3 tasks complete:
- `bun run typecheck && bun run build` exits 0
- All 11 URLs render correctly per the human checkpoint (4 BB with TabBar per D-06, 5 legacy with legacy chrome per D-04, 2 standalone with neither per D-02)
- D-06: `src/app/(bb)/layout.tsx` exists, server-only, composes `<PageShell>{children}<TabBar /></PageShell>` exactly
- D-07: Home and Settings live under `(bb)/`, both with zero `<main>` tags inside (PageShell provides it)
- D-07 + D-08: Stubs `/budgets` and `/transactions` ship with locked copy, no decorative chrome, SECURITY NOTE comments
- D-13: `src/middleware.ts` unchanged
- D-14: `/dashboard` 308 redirect works end-to-end (curl + browser confirmed)
- Active-tab highlighting works on all 4 BB routes per D-10 (visual confirm via checkpoint)
- TabBar absent on all 5 legacy routes (D-04 isolation) and both standalone routes (D-02) (visual confirm via checkpoint)
</verification>

<success_criteria>
- [ ] D-06: `src/app/(bb)/layout.tsx` server component composes <PageShell>{children}<TabBar /></PageShell>
- [ ] D-07: Home `src/app/page.tsx` relocated to `src/app/(bb)/page.tsx`; outer `<main>` downgraded to `<div>` (zero `<main>` tags in the page)
- [ ] D-07: Settings `src/app/settings/page.tsx` relocated to `src/app/(bb)/settings/page.tsx`; same `<main>` → `<div>` downgrade; body content otherwise unchanged ("existing settings stays untouched")
- [ ] D-07 + D-08: `src/app/(bb)/budgets/page.tsx` stub created with PageHeader + locked D-08 copy + SECURITY NOTE comment
- [ ] D-07 + D-08: `src/app/(bb)/transactions/page.tsx` stub created with PageHeader + locked D-08 copy + SECURITY NOTE comment
- [ ] `bun run build` passes; all 4 BB + 5 legacy + 2 standalone URLs resolve
- [ ] Human smoke test passes for all 11 URLs
- [ ] D-06: TabBar visible on `/`, `/budgets`, `/transactions`, `/settings`
- [ ] D-04: TabBar absent on `/legacy`, `/analytics`, `/achievements`, `/dashboard`, `/legacy-index`
- [ ] D-02: TabBar absent on `/login`, `/link-bank` (NAV-06)
- [ ] D-10: Active-tab highlighting works on each BB URL (`--bb-info` color, filled icon) (NAV-02)
- [ ] D-10: Tab switches are instant — no transition animation between routes (NAV-03)
- [ ] D-07: Git history records Home and Settings as renames (preserves blame)
- [ ] D-13: `src/middleware.ts` byte-identical to pre-Plan-03 state
- [ ] D-14: `/dashboard` 308 redirect to `/` confirmed via curl + browser in smoke test
</success_criteria>

<output>
Create `.planning/phases/07-layout-shell-navigation/07-04-SUMMARY.md` when done. Summary must include:
- Files created/modified/moved (paths)
- D-07: Confirmation of zero `<main>` tags in moved Home and Settings pages (paste `grep -c '<main\|</main>'` output for each)
- D-06: Confirmation that build lists all 11 URLs (paste relevant build-log lines)
- D-14: Result of `curl -I http://localhost:3031/dashboard` showing 308 still works (paste exact output)
- D-07: Result of `git log --diff-filter=R --name-status -1 -- "src/app/(bb)/page.tsx"` confirming Home move is a rename
- Checkpoint user-feedback transcript (paste the user's "approved" message — or document any visual issues noted for follow-up)
- Note any RESEARCH Open Q2 visual issues with lucide filled icons (which icon, what symptom) — these are documented follow-ups, not Phase 7 blockers
- D-13: Confirm that `src/middleware.ts` is untouched: paste `git diff HEAD src/middleware.ts` output (expected: empty)
- Decision coverage: D-01 (completed via (bb)/ creation), D-02, D-06, D-07, D-08, D-13, D-14 implemented or verified in this plan
- Note that Phase 7 is now complete — all 6 NAV-* requirements satisfied; ready for `/gsd:verify-work 7`
</output>
