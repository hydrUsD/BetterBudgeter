---
phase: 02-ui-library-strategy
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-tremor-migration-strategy/MIGRATION_RISK_ASSESSMENT.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "Migration risks are identified with mitigation strategies"
    - "CLAUDE.md contains library boundary rules for future Claude sessions"
    - "Risk assessment covers Tremor removal, Base UI adoption, and legacy isolation"
  artifacts:
    - path: ".planning/phases/02-tremor-migration-strategy/MIGRATION_RISK_ASSESSMENT.md"
      provides: "Risk analysis for Phase 3 migration"
      contains: "DonutChart"
    - path: "CLAUDE.md"
      provides: "Updated project rules with library boundaries"
      contains: "Base UI"
  key_links:
    - from: "MIGRATION_RISK_ASSESSMENT.md"
      to: "LIBRARY_STRATEGY.md"
      via: "risks derived from strategy decisions"
      pattern: "strategy"
    - from: "CLAUDE.md"
      to: "LIBRARY_STRATEGY.md"
      via: "boundary rules codified in project memory"
      pattern: "boundary"
---

<objective>
Create migration risk assessment and update CLAUDE.md with library boundary rules.

Purpose: The risk assessment prepares Phase 3 by identifying what could go wrong. The CLAUDE.md update ensures all future Claude sessions enforce the new library boundaries automatically.

Output: MIGRATION_RISK_ASSESSMENT.md and updated CLAUDE.md with library boundary section.
</objective>

<execution_context>
@/Users/paulheuwer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulheuwer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tremor-migration-strategy/02-CONTEXT.md
@.planning/phases/02-tremor-migration-strategy/02-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Migration Risk Assessment</name>
  <files>.planning/phases/02-tremor-migration-strategy/MIGRATION_RISK_ASSESSMENT.md</files>
  <action>
Create a migration risk assessment for Phase 3 work. Cover these areas:

1. **Tremor Removal Risks**:
   - DonutChart replacement: data format differences between Tremor DonutChart and shadcn/ui pie chart (Recharts). Reference the rollback procedure from 02-01-SUMMARY.md.
   - Tailwind config cleanup: risk of removing CSS utilities still used elsewhere. Mitigation: grep for usage before removing.
   - Package removal: any transitive dependencies that other packages rely on.

2. **Base UI Adoption Risks**:
   - v1.x maturity: package is relatively new (v1.0 Dec 2025). Risk: API changes in minor versions.
   - Mitigation: pin version, only use for primitives shadcn/ui doesn't cover.

3. **Legacy Isolation Risks**:
   - Radix version conflicts: legacy uses Radix directly, shadcn/ui bundles its own Radix. Risk: version mismatches.
   - Mitigation: shadcn/ui copies components locally, so Radix versions are independent.

4. **Risk Matrix** (table):
   | Risk | Likelihood | Impact | Mitigation |
   Format each risk as a row.

5. **Verification Strategy**: What to check after Phase 3 (build, typecheck, visual spot-check of DonutChart replacement).

Keep it concise — this is a planning artifact, not a formal risk report.
  </action>
  <verify>Document exists with risk matrix table. Contains sections for Tremor, Base UI, and legacy isolation risks.</verify>
  <done>All migration risks identified with likelihood, impact, and mitigation strategies.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with Library Boundary Rules</name>
  <files>CLAUDE.md</files>
  <action>
Add a new section to CLAUDE.md under "Fixed Architecture Decisions" called "### UI Library Boundaries". Include:

```markdown
### UI Library Boundaries

The project uses multiple UI libraries with strict separation rules:

| Library | Scope | Status |
|---------|-------|--------|
| shadcn/ui | All new BetterBudgeter components | Primary framework |
| Base UI (@base-ui/react) | Headless primitives when shadcn/ui lacks coverage | Secondary (new BB only) |
| Radix UI | Legacy OopsBudgeter components only | Frozen — do not extend |
| Recharts (via shadcn/ui charts) | All new charts | Active |
| Sonner | Notifications | Active |

**Strict rules:**
- BetterBudgeter components must NEVER import from `@radix-ui` directly
- Legacy OopsBudgeter pages stay on Radix only — no shadcn/ui adoption
- New charts must use shadcn/ui chart components (Recharts under the hood)
- Base UI is only used when shadcn/ui does not provide the needed primitive
- Tremor is removed — do not reintroduce
```

Place this section after the "### Tech Stack" section. Read CLAUDE.md first to find the exact insertion point.

Do NOT modify any other sections of CLAUDE.md.
  </action>
  <verify>
Grep CLAUDE.md for "UI Library Boundaries" — section must exist. Grep for "BetterBudgeter components must NEVER import from" — rule must exist. Run `bun run build` to ensure no issues.
  </verify>
  <done>CLAUDE.md contains library boundary rules that future Claude sessions will automatically enforce.</done>
</task>

</tasks>

<verification>
- MIGRATION_RISK_ASSESSMENT.md exists with risk matrix
- CLAUDE.md contains new "UI Library Boundaries" section
- No other CLAUDE.md sections were modified
- bun run build passes
</verification>

<success_criteria>
- Risk assessment covers all three areas (Tremor, Base UI, legacy isolation)
- CLAUDE.md library boundaries match the locked decisions from CONTEXT.md
- Both documents are ready to guide Phase 3 execution
</success_criteria>

<output>
After completion, create `.planning/phases/02-tremor-migration-strategy/02-03-SUMMARY.md`
</output>
