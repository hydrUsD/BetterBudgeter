---
phase: 02-ui-library-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-tremor-migration-strategy/UI_COMPONENT_INVENTORY.md
  - .planning/phases/02-tremor-migration-strategy/LIBRARY_STRATEGY.md
autonomous: true

must_haves:
  truths:
    - "Every UI component in the codebase is mapped to its source library"
    - "Library boundary rules are documented with clear rationale"
    - "Legacy vs BetterBudgeter components are clearly separated in the inventory"
  artifacts:
    - path: ".planning/phases/02-tremor-migration-strategy/UI_COMPONENT_INVENTORY.md"
      provides: "Complete component-to-library mapping"
      contains: "SpendingByCategoryChart"
    - path: ".planning/phases/02-tremor-migration-strategy/LIBRARY_STRATEGY.md"
      provides: "Library architecture decisions and boundary rules"
      contains: "shadcn/ui"
  key_links:
    - from: "UI_COMPONENT_INVENTORY.md"
      to: "LIBRARY_STRATEGY.md"
      via: "inventory informs strategy recommendations"
      pattern: "inventory"
---

<objective>
Create the full UI component inventory and library strategy document for BetterBudgeter.

Purpose: These documents capture the current state of all UI components and codify the library architecture decisions made during Phase 2 context discussion. They serve as the reference for Phase 3 migration work.

Output: Two documents — UI_COMPONENT_INVENTORY.md (every component mapped to its library) and LIBRARY_STRATEGY.md (the new library architecture and boundary rules).
</objective>

<execution_context>
@/Users/paulheuwer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulheuwer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tremor-migration-strategy/02-CONTEXT.md
@.planning/phases/02-tremor-migration-strategy/02-RESEARCH.md
@.planning/phases/02-tremor-migration-strategy/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI Component Inventory</name>
  <files>.planning/phases/02-tremor-migration-strategy/UI_COMPONENT_INVENTORY.md</files>
  <action>
Scan the entire codebase to build a comprehensive component inventory. The document must contain:

1. **shadcn/ui components** (src/components/ui/): List all 21 installed components with their file paths
2. **BetterBudgeter components**: List all BB-specific components (SpendingByCategoryChart, BudgetNotificationDialogs, SyncTransactionsButton, BudgetSettings, LinkBankFlow, LoginForm, SignOutButton) with which library each uses
3. **Legacy OopsBudgeter components** (src/components/legacy/): List all components with which library primitives they use (Radix, etc.)
4. **Tremor usage**: Document the single DonutChart usage in SpendingByCategoryChart.tsx — mark as TO MIGRATE
5. **Recharts usage**: Document Analytics.tsx usage — mark as FROZEN (legacy)
6. **Migration candidates**: Flag which components need changes in Phase 3

Format as markdown tables. Group by: shadcn/ui installed, BetterBudgeter custom, Legacy OopsBudgeter, Charts.

Scan actual files — do not rely solely on research notes. Use glob/grep to find all component files and their imports.
  </action>
  <verify>
Document exists and contains entries for all component directories. Grep the document for key components: "DonutChart", "SpendingByCategoryChart", "Analytics", "button", "card".
  </verify>
  <done>Every UI component in the codebase appears in the inventory with its source library identified.</done>
</task>

<task type="auto">
  <name>Task 2: Create Library Strategy Document</name>
  <files>.planning/phases/02-tremor-migration-strategy/LIBRARY_STRATEGY.md</files>
  <action>
Create the library architecture decision document. This codifies the decisions from CONTEXT.md into a developer-facing reference. Include:

1. **Current State Summary**: Brief overview referencing the component inventory
2. **Decision: Library Architecture** (table format):
   - shadcn/ui — primary framework for all new BB components
   - Base UI (@base-ui/react) — headless primitives when shadcn/ui doesn't cover a need
   - Radix UI — legacy OopsBudgeter only, frozen
   - Recharts (via shadcn/ui charts) — all new charts
   - Sonner — notifications (already in use)
   - Tremor — REMOVE (unmaintained, no commits in 1+ year)

3. **Boundary Rules** (the strict rules from CONTEXT.md):
   - BetterBudgeter components must never import from @radix-ui directly
   - Legacy pages stay on Radix only, no shadcn/ui adoption
   - New charts use shadcn/ui charts only
   - Base UI only when shadcn/ui lacks coverage

4. **Tremor Removal Scope**: Reference 02-01 audit findings — 1 component (DonutChart), Tailwind config cleanup needed (globals.css lines 5-53)

5. **Migration Path**: Brief description of what Phase 3 will do (replace DonutChart, remove Tremor dep, clean Tailwind config)

Audience: developer and Claude. Keep it concise and actionable.
  </action>
  <verify>
Document exists. Contains sections for each library decision. Contains boundary rules. References Tremor removal scope.
  </verify>
  <done>Library architecture decisions are fully documented with boundary rules and migration path.</done>
</task>

</tasks>

<verification>
- Both documents exist in .planning/phases/02-tremor-migration-strategy/
- UI_COMPONENT_INVENTORY.md lists components from all directories (ui/, legacy/, BB custom, charts)
- LIBRARY_STRATEGY.md contains all 6 library decisions and boundary rules
- bun run build passes (no code changes, but verify project still builds)
</verification>

<success_criteria>
- Complete component inventory covering every UI component in the codebase
- Library strategy document with all locked decisions from CONTEXT.md
- Both documents are actionable references for Phase 3 migration
</success_criteria>

<output>
After completion, create `.planning/phases/02-tremor-migration-strategy/02-02-SUMMARY.md`
</output>
