---
phase: 02-tremor-migration-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-tremor-migration-strategy/TREMOR_AUDIT.md
  - .planning/phases/02-tremor-migration-strategy/TREMOR_STABILITY_STRATEGY.md
autonomous: true

must_haves:
  truths:
    - "Every Tremor import in the codebase is documented with file path, component, and props used"
    - "API differences between v3 stable and v4 beta are documented with before/after examples"
    - "A rollback procedure exists for replacing Tremor DonutChart with direct recharts"
    - "A monitoring checklist exists for tracking v4 stable release"
  artifacts:
    - path: ".planning/phases/02-tremor-migration-strategy/TREMOR_AUDIT.md"
      provides: "Complete Tremor usage audit and API documentation"
      contains: "DonutChart"
    - path: ".planning/phases/02-tremor-migration-strategy/TREMOR_STABILITY_STRATEGY.md"
      provides: "Rollback procedures and monitoring plan"
      contains: "rollback"
  key_links:
    - from: "TREMOR_AUDIT.md"
      to: "src/components/dashboard/SpendingByCategoryChart.tsx"
      via: "documents actual usage"
      pattern: "SpendingByCategoryChart"
---

<objective>
Audit all Tremor usage in the codebase and create stability/rollback documentation for the v4 beta dependency.

Purpose: Research revealed that the "v1.0.0 migration" target doesn't exist as an npm package. The project is already on v4 beta (the only React 19 compatible version). This plan documents the current state and creates a safety net.

Output: Two documents — a usage audit and a stability strategy with rollback procedures.
</objective>

<execution_context>
@/Users/paulheuwer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulheuwer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tremor-migration-strategy/02-RESEARCH.md
@.planning/phases/02-tremor-migration-strategy/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tremor Usage Audit and API Documentation</name>
  <files>.planning/phases/02-tremor-migration-strategy/TREMOR_AUDIT.md</files>
  <action>
    Scan the entire codebase for ALL Tremor-related imports, usage, and references:

    1. Search for every `@tremor/react` import across all .ts/.tsx files
    2. For each import found, document:
       - File path
       - Component/type imported
       - Every prop used with its value
       - The page/route where it renders
    3. Search for Tremor color names usage (the named color palette)
    4. Check legacy/ components too (verify they do NOT use Tremor)
    5. Check utils/charts/ for Tremor-related helpers
    6. Document the installed version from package.json

    Then create API difference documentation:
    - v3.18.7 stable vs v4.0.0-beta for each component found
    - Use before/after code examples (junior dev audience)
    - Note which props changed, were added, or removed
    - Reference the TypeScript definitions from node_modules/@tremor/react/dist/index.d.ts

    Write everything to TREMOR_AUDIT.md with these sections:
    - Summary (component count, version, status)
    - Usage Inventory (table: file, component, props)
    - API Differences (v3 vs v4 with code examples)
    - Color System Documentation (named colors, dual-system explanation)
    - Transitive Dependencies (recharts version, tailwind-variants)
  </action>
  <verify>
    - TREMOR_AUDIT.md exists and contains all sections listed above
    - grep -r "@tremor/react" src/ matches what the audit documents (no missed imports)
    - API differences section includes before/after code examples
  </verify>
  <done>Every Tremor import, component, and prop in the codebase is documented. API differences between v3 and v4 are explained with code examples a junior developer can follow.</done>
</task>

<task type="auto">
  <name>Task 2: Stability Strategy and Rollback Documentation</name>
  <files>.planning/phases/02-tremor-migration-strategy/TREMOR_STABILITY_STRATEGY.md</files>
  <action>
    Create TREMOR_STABILITY_STRATEGY.md with these sections:

    1. **Current State Assessment**
       - Version installed (4.0.0-beta-tremor-v4.4)
       - Why this version (React 19 + Tailwind v4 compatibility)
       - Why v3 stable is NOT an option (incompatible stack)
       - Why "v1.0.0" target was a misunderstanding (document for team clarity)

    2. **Risk Assessment**
       - Risk level per component (only DonutChart — assess stability)
       - Beta API change risk (what could break in future beta updates)
       - Dependency chain risk (recharts, tailwind-variants)

    3. **Rollback Procedure: DonutChart to Recharts PieChart**
       - Step-by-step instructions to replace DonutChart with recharts PieChart
       - Include actual code example using recharts API (recharts is already installed as transitive dep)
       - Map current props to recharts equivalents
       - Note: CATEGORY_COLORS in utils/charts/ already has HEX values for recharts
       - Estimated effort: 1-2 hours

    4. **Monitoring Plan**
       - Where to check for v4 stable release (GitHub releases, npm registry)
       - Quarterly review checklist (what to verify every 3 months)
       - Decision criteria: when to upgrade, when to switch to Raw, when to use recharts directly

    5. **Recommendation for Phases 3-4**
       - Phase 3 (Tremor Migration Execution) should be SKIPPED or REDEFINED — no migration needed
       - Phase 4 (Library Consolidation) can proceed as planned
       - Suggest updated roadmap language

    Write for junior developers — explain WHY each recommendation is made.
  </action>
  <verify>
    - TREMOR_STABILITY_STRATEGY.md exists with all 5 sections
    - Rollback procedure includes actual recharts code example
    - Monitoring plan has concrete check URLs and review schedule
    - Phase 3/4 recommendations are actionable
  </verify>
  <done>Team has a clear stability strategy: knows current risks, has a tested rollback path, knows when/where to monitor for updates, and understands how this affects the remaining roadmap phases.</done>
</task>

</tasks>

<verification>
- Both documents exist in .planning/phases/02-tremor-migration-strategy/
- TREMOR_AUDIT.md covers every @tremor/react import in the codebase (verified by grep)
- TREMOR_STABILITY_STRATEGY.md includes a working recharts rollback code example
- Documents are written at junior developer comprehension level
</verification>

<success_criteria>
- Complete Tremor usage inventory with zero missed imports
- API differences documented with before/after code examples
- Rollback procedure with actual recharts replacement code
- Monitoring plan with specific URLs and quarterly schedule
- Clear recommendation on Phase 3 (skip/redefine)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tremor-migration-strategy/02-01-SUMMARY.md`
</output>
