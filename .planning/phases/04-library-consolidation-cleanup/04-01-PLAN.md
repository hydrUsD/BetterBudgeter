---
phase: 04-library-consolidation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils.ts
  - src/components/dashboard/index.ts
  - src/utils/charts/index.ts
  - src/app/page.tsx
  - package.json
  - bun.lockb
autonomous: true

must_haves:
  truths:
    - "No Tremor references remain in source code"
    - "Unused Tremor CSS utilities are removed from utils.ts"
    - "Unused dependencies are removed from package.json"
    - "Build succeeds after all cleanup"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "Clean utility exports without Tremor artifacts"
      not_contains: "Tremor"
    - path: "src/components/dashboard/index.ts"
      provides: "Accurate library documentation in comments"
      contains: "Recharts"
    - path: "package.json"
      provides: "Clean dependency list"
      not_contains: ["mongoose", "quick.db"]
  key_links:
    - from: "src/components/dashboard/SpendingByCategoryChart.tsx"
      to: "src/lib/utils.ts"
      via: "cn() utility"
      pattern: "import.*cn.*from.*utils"
---

<objective>
Clean up all remaining Tremor artifacts and unused dependencies from the codebase.

Purpose: Complete the technical debt cleanup from Phase 03 by removing leftover Tremor utilities, updating outdated comments, and removing unused npm dependencies (mongoose, quick.db).

Output: A clean codebase with no Tremor references and no unused dependencies, ready for documentation in Plan 02.
</objective>

<execution_context>
@/Users/paulheuwer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulheuwer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-library-consolidation-cleanup/04-CONTEXT.md
@.planning/phases/04-library-consolidation-cleanup/04-RESEARCH.md
@.planning/phases/03-ui-library-migration/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Tremor utilities from utils.ts</name>
  <files>src/lib/utils.ts</files>
  <action>
Remove the unused Tremor CSS utility exports from src/lib/utils.ts:

1. Delete lines 8-37 which contain:
   - `focusInput` array (lines 8-17)
   - `focusRing` array (lines 19-26)
   - `hasErrorInput` array (lines 28-37)

2. Keep only the `cn()` function (lines 1-6)

Research confirmed these utilities are NOT imported anywhere in the codebase - only the definition exists in utils.ts.

The file should be reduced to just:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
  </action>
  <verify>
- `grep -r "focusInput\|focusRing\|hasErrorInput" src/` returns only the definition (if any) or nothing
- `wc -l src/lib/utils.ts` shows ~6 lines
- `bun run typecheck` passes
  </verify>
  <done>Tremor CSS utilities removed from utils.ts, file contains only cn() function</done>
</task>

<task type="auto">
  <name>Task 2: Update outdated Tremor comments in source files</name>
  <files>src/components/dashboard/index.ts, src/utils/charts/index.ts, src/app/page.tsx</files>
  <action>
Update comments that still reference Tremor to reflect the current Recharts/shadcn-ui reality:

**File 1: src/components/dashboard/index.ts**
- Line 32: Change "Uses Tremor for visualization" to "Uses Recharts (via shadcn/ui charts) for visualization"

**File 2: src/utils/charts/index.ts**
- Line 4: Change "(Tremor/Recharts)" to "(Recharts via shadcn/ui)"

**File 3: src/app/page.tsx**
- Line 205: Change "(Tremor Donut Chart)" to "(Recharts PieChart via shadcn/ui)"

After updates, run a comprehensive grep to verify no Tremor references remain:
```bash
grep -ri "tremor" src/ --include="*.ts" --include="*.tsx"
```

Expected: No matches (or only false positives like variable names unrelated to the library).
  </action>
  <verify>
- `grep -ri "tremor" src/ --include="*.ts" --include="*.tsx"` returns no matches
- Comments accurately describe current library usage
  </verify>
  <done>All source file comments updated to reflect Recharts/shadcn-ui, no Tremor references in src/</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused dependencies</name>
  <files>package.json, bun.lockb</files>
  <action>
Remove known unused dependencies from package.json:

1. Run dependency audit to confirm unused deps:
   ```bash
   bunx depcheck
   ```

2. Remove confirmed unused dependencies:
   ```bash
   bun remove mongoose quick.db
   ```

3. Verify removal succeeded:
   - Check package.json no longer contains mongoose or quick.db
   - Run `bun install` to ensure lockfile is consistent

**Important:** Review depcheck output carefully before removing anything beyond mongoose and quick.db. If depcheck flags other "unused" dependencies:
- Check if they are peer dependencies
- Check if they are Tailwind/PostCSS plugins
- Check if they are runtime-only dependencies
- DO NOT remove them without explicit verification

Document any additional outdated dependencies found in the task summary for future consideration (do not remove them in this task).
  </action>
  <verify>
- `grep "mongoose\|quick.db" package.json` returns no matches
- `bun run build` succeeds
- `bun run typecheck` passes
  </verify>
  <done>mongoose and quick.db removed from dependencies, build still succeeds</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **No Tremor references in source:**
   ```bash
   grep -ri "tremor" src/ --include="*.ts" --include="*.tsx"
   ```
   Expected: No matches

2. **Clean utils.ts:**
   ```bash
   cat src/lib/utils.ts | head -10
   ```
   Expected: Only cn() function, no focusInput/focusRing/hasErrorInput

3. **Clean dependencies:**
   ```bash
   grep -E "mongoose|quick\.db" package.json
   ```
   Expected: No matches

4. **Build verification:**
   ```bash
   bun run typecheck && bun run build
   ```
   Expected: Both pass

5. **No regression in cn() usage:**
   ```bash
   grep -r "import.*cn.*from.*utils" src/ | head -5
   ```
   Expected: Imports still work (confirms cn() wasn't broken)
</verification>

<success_criteria>
- All Tremor references removed from source code
- utils.ts contains only cn() function (no focusInput, focusRing, hasErrorInput)
- mongoose and quick.db removed from package.json
- All outdated comments updated to reflect Recharts/shadcn-ui
- Build and typecheck pass
- No functional regression
</success_criteria>

<output>
After completion, create `.planning/phases/04-library-consolidation-cleanup/04-01-SUMMARY.md`
</output>
