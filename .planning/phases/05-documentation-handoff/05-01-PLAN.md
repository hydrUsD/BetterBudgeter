---
phase: 05-documentation-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/UI_AUDIT_HANDOFF.md
autonomous: true

must_haves:
  truths:
    - "User can understand project state in under 10 minutes by reading docs/UI_AUDIT_HANDOFF.md"
    - "User can see which components are active vs legacy vs redesign-target in one scannable table"
    - "User can visualize route and folder boundaries with clear zone labels"
    - "User can evaluate current ADHD UX compliance with specific examples"
    - "User can make informed single-page vs multi-page dashboard decision using this document"
  artifacts:
    - path: "docs/UI_AUDIT_HANDOFF.md"
      provides: "Comprehensive UI audit and handoff documentation"
      min_lines: 200
      contains:
        - "## Quick Start"
        - "## Component Inventory"
        - "## Boundary Maps"
        - "## ADHD UX Evaluation"
        - "## Figma Handoff Notes"
  key_links:
    - from: "docs/UI_AUDIT_HANDOFF.md"
      to: "docs/UI_ARCHITECTURE.md"
      via: "cross-reference link"
      pattern: "\\[UI_ARCHITECTURE\\.md\\]"
    - from: "docs/UI_AUDIT_HANDOFF.md"
      to: "CLAUDE.md"
      via: "cross-reference link"
      pattern: "\\[CLAUDE\\.md\\]"
---

<objective>
Create comprehensive UI audit and handoff documentation for BetterBudgeter.

Purpose: Enable rapid context restoration (under 10 minutes) and support major UI/UX restructuring decisions, specifically whether to maintain single-page dashboard or split into multiple pages.

Output: Single file `docs/UI_AUDIT_HANDOFF.md` containing component inventory, boundary maps, ADHD UX evaluation, and Figma handoff notes.
</objective>

<execution_context>
@/Users/paulheuwer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulheuwer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-handoff/05-CONTEXT.md
@.planning/phases/05-documentation-handoff/05-RESEARCH.md

# Existing documentation to cross-reference (do not duplicate)
@docs/UI_ARCHITECTURE.md
@CLAUDE.md

# Source files to verify inventory against
@src/components/dashboard/
@src/components/settings/
@src/components/auth/
@src/components/finance/
@src/components/common/
@src/components/ui/
@src/components/legacy/
@src/app/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI_AUDIT_HANDOFF.md with Inventory and Boundary Maps</name>
  <files>docs/UI_AUDIT_HANDOFF.md</files>
  <action>
Create `docs/UI_AUDIT_HANDOFF.md` with the following structure:

**Header Section:**
- Document title: "UI Audit & Handoff Documentation"
- Created date and last verified date
- Purpose statement: rapid context restoration and redesign decision support
- Link to related docs (UI_ARCHITECTURE.md for library details, CLAUDE.md for rules)

**Quick Start Section (< 10 min orientation):**
- Current state summary (5-7 bullet points covering: milestone completed, library stack, boundaries established)
- Key constraints (what cannot change: legacy zone frozen, library boundaries, auth flow)
- Primary redesign target (Dashboard at `/`)
- What this document enables: single-page vs multi-page dashboard decision

**Component Inventory Section:**
- Table with columns: Component | Library | Location | Status | Complexity
- Sort order: ACTIVE/REDESIGN-TARGET components first, LEGACY at bottom
- Complexity criteria in caption: simple (<100 LOC, single responsibility), moderate (100-300 LOC OR multiple concerns), complex (>300 LOC OR multiple concerns AND external state)

Verify inventory by scanning actual `src/components/` directories. Include ALL components from:
- dashboard/ (4 components, REDESIGN-TARGET)
- settings/ (1 component, ACTIVE)
- auth/ (2 components, ACTIVE)
- finance/ (1 component, ACTIVE)
- common/ (1 component, ACTIVE)
- ui/ (19 shadcn/ui primitives, ACTIVE)
- legacy/ (29 components, LEGACY)

**Boundary Maps Section:**
1. ASCII folder tree with zone annotations:
   - REDESIGN-TARGET: components/dashboard/
   - ACTIVE: components/settings/, auth/, finance/, common/, ui/
   - LEGACY: components/legacy/

2. Mermaid route diagram with zone labels and styling:
   - REDESIGN-TARGET (green): / (Dashboard)
   - ACTIVE (blue): /settings, /link-bank, /login
   - LEGACY (amber): /legacy, /analytics, /achievements, /legacy-index
   - Include /dashboard -> / redirect notation

Use styling: green (#10b981) for redesign-target, blue (#3b82f6) for active, amber (#f59e0b) for legacy.

**Do NOT:**
- Duplicate library decision content from UI_ARCHITECTURE.md (link to it instead)
- Duplicate rules from CLAUDE.md (link to it instead)
- Include code snippets (that belongs in code comments)
  </action>
  <verify>
1. File exists: `ls docs/UI_AUDIT_HANDOFF.md`
2. Contains required sections: `grep -E "^## (Quick Start|Component Inventory|Boundary Maps)" docs/UI_AUDIT_HANDOFF.md`
3. Contains cross-references: `grep -E "\[UI_ARCHITECTURE\.md\]|\[CLAUDE\.md\]" docs/UI_AUDIT_HANDOFF.md`
4. Component count matches filesystem: Compare table rows against `find src/components -name "*.tsx" | wc -l`
5. Mermaid diagram present: `grep -A5 "mermaid" docs/UI_AUDIT_HANDOFF.md`
  </verify>
  <done>
- docs/UI_AUDIT_HANDOFF.md exists with Quick Start, Component Inventory, and Boundary Maps sections
- Inventory table includes all components from src/components/ with correct status and complexity
- ASCII folder tree shows all directories with zone labels
- Mermaid route diagram shows all 9 routes with zone styling
- Cross-references to UI_ARCHITECTURE.md and CLAUDE.md are present
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ADHD UX Evaluation and Figma Handoff Notes</name>
  <files>docs/UI_AUDIT_HANDOFF.md</files>
  <action>
Add two sections to docs/UI_AUDIT_HANDOFF.md:

**ADHD UX Evaluation Section:**

Evaluate current UI against ADHD design principles from CLAUDE.md and research:

1. **Visual Clarity and Minimal Distractions**
   - Evaluate Dashboard (/): Does it follow "few elements, high signal"?
   - Cite specific compliance: e.g., "SpendingByCategoryChart uses single donut with clear legend"
   - Cite specific violations or opportunities: e.g., "Dashboard shows [X] sections simultaneously - potential information overload"

2. **Predictable Structure**
   - Evaluate navigation patterns
   - Assess consistency of component layouts

3. **Clear Defaults and Minimal Configuration**
   - How well does current UI minimize decisions?
   - Example: Budget settings require explicit setup vs smart defaults

4. **Progress Indicators and Feedback**
   - Are loading states clear?
   - How well does Sonner toast system support this?

5. **Empty States Guide Users**
   - Are there helpful CTAs instead of blank UI?
   - Cite examples of good/bad empty states

For each principle, use format:
- **Principle:** [name]
- **Current:** [GOOD/NEEDS-WORK/VIOLATION]
- **Evidence:** [specific component or screen]
- **Redesign opportunity:** [if applicable]

**Redesign Opportunities Summary:**
- List top 3-5 opportunities identified from ADHD evaluation
- Prioritize by impact on cognitive load reduction

**Figma Handoff Notes Section:**

1. **Design Constraints:**
   - Technical: shadcn/ui component library (Tailwind-based), Recharts for charts
   - Boundary: Legacy zone is frozen, cannot redesign /legacy, /analytics, /achievements
   - Auth: Supabase Auth flow must remain (login page structure)

2. **Visual Baseline Reference:**
   - Colors: Reference `src/app/globals.css` for CSS variables (do not extract, just point to file)
   - Chart colors: Reference `src/utils/charts/index.ts` CATEGORY_COLORS
   - Typography: Tailwind defaults

3. **Dashboard Focus Areas:**
   - Primary redesign target: / (main dashboard)
   - Key question: Single-page dashboard vs multi-page architecture?
   - Current dashboard sections: [list actual sections from dashboard page]
   - Candidate pages if splitting: e.g., Transactions, Budget Tracking, Spending Analysis

4. **Decision Support Matrix:**
   - Table comparing single-page vs multi-page approach
   - Columns: Aspect | Single-Page | Multi-Page
   - Rows: ADHD impact, Navigation complexity, Development effort, User context switching

**Do NOT:**
- Make the decision for the user (document enables decision, doesn't prescribe it)
- Include implementation details (this is design handoff, not dev spec)
- Duplicate principles already in CLAUDE.md (reference and apply them)
  </action>
  <verify>
1. ADHD section exists: `grep "^## ADHD UX Evaluation" docs/UI_AUDIT_HANDOFF.md`
2. Contains specific examples: `grep -E "(GOOD|NEEDS-WORK|VIOLATION)" docs/UI_AUDIT_HANDOFF.md | wc -l` should be >= 5
3. Figma section exists: `grep "^## Figma Handoff Notes" docs/UI_AUDIT_HANDOFF.md`
4. Decision matrix present: `grep -A10 "Decision Support Matrix" docs/UI_AUDIT_HANDOFF.md`
5. File validates as markdown: No syntax errors when rendered
  </verify>
  <done>
- ADHD UX Evaluation section evaluates 5 principles with GOOD/NEEDS-WORK/VIOLATION ratings
- Each evaluation includes specific evidence from current codebase
- Redesign Opportunities summary lists top 3-5 items prioritized by impact
- Figma Handoff Notes includes design constraints, visual baseline references, and dashboard focus areas
- Decision Support Matrix compares single-page vs multi-page approaches
- Document remains under 500 lines (as per research pitfall guidance)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cat docs/UI_AUDIT_HANDOFF.md | wc -l` should be 200-500 lines
2. Document structure: All 5 main sections present (Quick Start, Component Inventory, Boundary Maps, ADHD UX Evaluation, Figma Handoff Notes)
3. Cross-references work: Links to UI_ARCHITECTURE.md and CLAUDE.md are correct relative paths
4. No duplication: Document links to rather than copies from existing docs
5. `bun run build` passes (documentation doesn't break build)
</verification>

<success_criteria>
- Single comprehensive docs/UI_AUDIT_HANDOFF.md file exists
- Document enables context restoration in under 10 minutes (Quick Start section)
- Component inventory is complete and verified against filesystem
- Boundary maps clearly label all zones (ACTIVE, LEGACY, REDESIGN-TARGET)
- ADHD evaluation provides specific compliance/violation examples
- Figma notes enable single-page vs multi-page dashboard decision
- Cross-references to existing documentation are present (no duplication)
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-handoff/05-01-SUMMARY.md`
</output>
